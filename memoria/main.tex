% ======================================================================
\documentclass[12pt,a4paper,twoside,openright]{book}

%───────── Auxiliary packages ──────────────────────────────────────────
\usepackage{blindtext}
\usepackage{mwe}
\usepackage{csquotes}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{siunitx}
\usepackage{amsmath,amssymb}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{listings}
\usepackage{nomencl}
\makenomenclature
\usepackage[
  backend=biber,
  style=numeric,
  sorting=none % Antes pone sorting=nyt --> Eso hace que se ordene según Name(autor), Year(año), Title(título)
  % Lo cambiamos para que aparezcan simplemente en el orden en que aparecen en references.bib
]{biblatex}
\usepackage{subcaption}
\setlength{\textfloatsep}{20pt} % Separación entre el fin de una imagen y el parrafo siguiente (sin esto, el hueco era demasiado grande)
\captionsetup[subfigure]{skip=2pt}
\addbibresource{references.bib}
\usepackage{enumitem}
\setlist{nosep} % Para evitar que entre los elementos de \itemize haya mucha distancia

% Listings config
\lstset{
  basicstyle=\ttfamily\small,
  numbers=left,
  numberstyle=\tiny,
  columns=fullflexible,
  frame=single,
  breaklines=true,
  tabsize=2
}

%───────── Template, covers, back cover ────────────────────────────────
\usepackage{template/sty_files/template-TFGspanish-uma}

\usepackage{template/sty_files/cotutor_cover-blueuma}
\usepackage{template/sty_files/cotutor_cover-whiteuma}

\usepackage{template/sty_files/backcover-uma}
%\usepackage{parskip}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.7em} % Separación entre párrafos

%======================================================================
\begin{document}

%────────────────────── Blue front cover (Cotutor) ───────────────────────

\MakeBlueUMACover{
  degree     = {Grado en Ingeniería Informática},
  mencion    = {Mención en Computación},
  titleES    = {Diseño e implementación de un agente de razonamiento basado en LLMs con exploración mediante Beam Search},
  titleEN    = {Design and implementation of an LLM-based reasoning agent with Beam Search exploration},
  author     = {Juan Torres Gómez},
  tutor      = {Sergio Gálvez Rojas},
  % cotutor= {Dra.~Jane Doe},
  dept       = {Departamento de Lenguajes y Ciencias de la Computación},
  cityDate   = {Málaga, Julio, 2026},
  logoLeft   = {template/logos/NEG-uma-logo.png},
  logoRight  = {template/logos/NEG-etsi-logo.png}
}

%────────────────────── White front cover (Cotutor) ───────────────────────
\MakeWhiteUMACover{
  degree     = {Grado en Ingeniería Informática},
  mencion    = {Mención en Computación},
  titleES    = {Diseño e implementación de un agente de razonamiento basado en LLMs con exploración mediante Beam Search},
  titleEN    = {Design and implementation of an LLM-based reasoning agent with Beam Search exploration},
  author     = {Juan Torres Gómez},
  tutor      = {Sergio Gálvez Rojas},
  % cotutor= {Dra.~Jane Doe},
  dept       = {Departamento de Lenguajes y Ciencias de la Computación},
  cityDate   = {Málaga, Julio, 2026},
  defenseDate = {Julio, 2026},
  logoLeft   = {template/logos/POS-uma-logo.png},
  logoRight  = {template/logos/POS-etsi-logo.png}
}

\frontmatter

%────────────────────── Abstracts ──────────────────────────────────────
\renewcommand{\abstractname}{Resumen}
\begin{abstract}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

EL RESUMEN MEJOR DEJARLO PARA EL FINAL, YA QUE TAMBIÉN SE DEBEN INCLUIR ASPECTOS RELACIONADOS CON LOS RESULTADOS

%En este trabajo se presenta el desarrollo de una prueba de concepto de un agente con capacidad de razonamiento basado en modelos de lenguaje de gran tamaño \emph{(LLMs)}.
%A diferencia de los sistemas tradicionales que generan una respuesta directa ante una consulta, el agente propuesto implementa bucles dinámicos de \emph{Chain of Thought (CoT)} que le permiten generar,
%evaluar y refinar pasos intermedios antes de producir una respuesta final.

%El sistema se implementa utilizando modelos de lenguaje ejecutados en local, como Llama-3, junto con la librería \emph{LangGraph} para la contrucción de flujos de razonamiento. Además, se emplea
%una estrategia de búsqueda basada en \emph{Beam Search} que permite explorar múltiples posibles líneas de razonamiento y seleccionar aquellas más prometedoras mediante un mecanismo de puntuación.

%El objetivo del proyecto es analizar la viabilidad de este enfoque para mejorar la capacidad de razonamiento de agentes basados en \emph{LLMs} y estudiar su comportamiento en diferentes escenarios de
%resolución de problemas.
%\medskip

%\noindent \textbf{Palabras clave:} modelos de lenguaje, agentes inteligentes, \emph{Chain of Thought}, razonamiento automático, \emph{Beam Search}.
\end{abstract}


\renewcommand{\abstractname}{Abstract}
\begin{abstract}
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}

EL RESUMEN MEJOR DEJARLO PARA EL FINAL, YA QUE TAMBIÉN SE DEBEN INCLUIR ASPECTOS RELACIONADOS CON LOS RESULTADOS

%This work represents the development of a proof-of-concept reasoning agent based on Large Language Models \emph{(LLMs)}.
%Unlike traditional systems that generate an immediate response to a query, the proposed agent implements dynamic \emph{Chain Of Thought (CoT)} loops that allow it to generate, evaluate, and refine
%intermediate reasoning steps before producing a final answer.

%The system is implemented using locally executed language models such as Llama-3, together with the \emph{LangGraph} library for building reasoning workflows. Additionally, a \emph{Beam Search}
%strategy is employed to explore multiple possible reasoning paths and select the most promising ones through a scoring mechanism.

%The goal of this project is to analyze the feasibility of this approach to improve the reasoning capabilities of LLM-based agents and to study their behaviour in different problem-solving scenarios.
%\medskip

%\noindent \textbf{Keywords:} language models, intelligent agents, \emph{Chain of Thought}, automated reasoning, \emph{Beam Search}.
\end{abstract}

%──────────────── Acknowledgments ───────────────────────────────────────

% ====================================== AGRADECIMIENTOS ====================================
\begin{umaacknowledgments}
ESTA PARTE ES LO ÚLTIMO DEL TFG
\end{umaacknowledgments}

%────────────────────── TOC and lists ──────────────────────────────────
\clearpage
\tableofcontents
\clearpage
\listoffigures
\clearpage
\listoftables
\clearpage

%────────────────────── Nomenclature ───────────────────────────────────
\nomenclature{$x \in \mathbb{R}^{H\times W\times D}$}{HR volume}
\nomenclature{$y \in \mathbb{R}^{h\times w\times d}$}{LR volume}
\nomenclature{$A$}{Forward operator (blur+downsample)}
\nomenclature{$\hat x$}{Network reconstruction}
\nomenclature{$\mathcal{L}$}{Training objective}
\nomenclature{$\operatorname{PSNR}$}{Peak Signal-to-Noise Ratio}
\nomenclature{$\operatorname{SSIM}$}{Structural Similarity}
\nomenclature{$\operatorname{Dice}$}{Sørensen–Dice coefficient}
\printnomenclature
\clearpage

\mainmatter
%────────────────────── Capítulo 1 — Introducción ───────────────────────
\chapter{Introducción}
\section{Motivación}
%\setlength{\parindent}{0pt}
%\setlength{\parskip}{0.5em}

En los últimos años, el desarrollo de los \emph{Large Language Models (LLMs)} ha supuesto un avance significativo en tareas de procesamiento
del lenguaje natural. Sin embargo, a pesar de la capacidad para generar texto coherente y resolver una amplia variedad de tareas, estos modelos
presentan limitaciones en lo que a enfrentar problemas que requieren un razonamiento complejo se refiere, ya que tienden a generar respuestas
directas sin una estructura intermedia explícita.

La ausencia de pasos intermedios limita la capacidad de valorar el razonamiento, desencadenando en falta de transparencia y control sobre cómo se
construye el razonamiento.
Es por ello que surge la necesidad de desarrollar sistemas que no solo generen respuestas, sino que estructuren su proceso de razonamiento y evalúen
sus propios pasos intermedios, con un enfoque que facilite la experimentación y análisis del comportamiento del modelo.

En conclusión, este proyecto se plantea a partir de la motivación por construir algo más complejo que una simple llamada a un modelo, profundizando
así en un área en crecimiento, desde el interés por entender no solo qué responden estos modelos, sino cómo llegan a hacerlo.

\section{Trabajos Previos}

Con el fin de abordar la limitación que presentan los \emph{Large Language Models (LLMs)} anteriormente mencionada, diversos trabajos han propuesto
técnicas que fomentan el razonamiento paso a paso. Un ejemplo de esto es \emph{"Chain-of-Thought Prompting (CoT)"}, introducido por Wei et al. (2022) [1],
el cual consiste en guiar al modelo para que genere pasos intermedios antes de alcanzar una respuesta final. Este enfoque pone de manifiesto la importancia
de estructurar el proceso de generación en lugar de limitarse a obtener una respuesta final directa.

Por otro lado, en el ámbito de la generación de texto, los algoritmos de búsqueda han sido ampliamente utilizados para explorar múltiples
posibles soluciones. Destaca el algoritmo de \emph{Beam Search}, el cual se apoya en una función de evaluación para mantener las secuencias más
prometedoras en cada iteración. Trabajos como el de Wu et al. (2020) [3] demuestran que, aunque este método haya sido tradicionalmente aplicado en tareas como la traducción automática,
su uso puede extenderse también al razonamiento en \emph{LLMs}, simplemente permitiendo explorar distintas cadenas de pensamiento
\emph{(CoT)} y seleccionando las más relevantes (según la función de evaluación anteriormente mencionada).

Estos antecedentes reflejan dos ideas fundamentales:
\begin{itemize}
  \item Por un lado, la necesidad de guiar el razonamiento de los modelos mediante la generación de pasos intermedios.
  \item Por el otro, la utilidad de explorar múltiples soluciones antes de seleccionar la respuesta final.
\end{itemize}

Sin embargo, la combinación de ambos enfoques en sistemas que implementen bucles dinámicos de razonamiento y mecanismos de selección de hipótesis
continua siendo un área en pleno desarrollo.

\section{Objetivos}

El objetivo principal de este trabajo es desarrollar una prueba de concepto de un agente basado en modelos de lenguaje capaz de incorporar un proceso de
razonamiento estructurado, en lugar de generar respuestas de forma directa. De esta forma, y con el objetivo de facilitar un aálisis detallado del proceso
seguido por el modelo, se busca explorar un enfoque que permita descomponer la resolución de un problema de índole lingüística en una serie de pasos intermedios.

De forma más concreta, se pretende implementar un sistema que no dependa únicamente de un camino de razonamiento, sino que sea capaz de generar múltiples posibles
trayectorias, permitiendo así analizar y estudiar como evoluciona el proceso de generación de cada una de ellas. Asimismo, un mecanismo evaluará la calidad de dichos
razonamientos generados, con el fin de seleccionar aquellos que resulten más adecuados con el tipo de problema que con el que se está trabajando. Este proceso de
evaluación se utilizará por tanto para guiar la exploración del espacio de soluciones, descartando las opciones menos prometedoras.

Otro gran objetivo de este proyecto se centra en la estructuración del funcionamiento del sistema mediante un flujo de ejecución organizado, que facilite no solo la
comprensión de este, sino también su posible extensión en trabajos futuros.

Por último, se busca analizar el comportamiento del agente desarrollado a través de problemas de índole lingüística...


\section{Estructura del Proyecto}

%────────────────────── Chapter 2 — Estado del arte ───────────────────────
\chapter{Estado del Arte}

%────────────────────── Introducción al problema del razonamiento en LLMs ──────────────────────────
\section{Introducción al Problema del Razonamiento en LLMs}

Los \emph{Large Language Models (LLMs)} han experimentado un crecimiento notable en los últimos años, convirtiéndose en una de las tecnologías más relevantes dentro del ámbito del procesamiento del lenguaje natural.
Modelos basados en arquitecturas \emph{Transformer} han demostrado una gran capacidad para generar texto coherente, mantener el contexto en conversaciones prolongadas y abordar una amplia variedad de tareas lingüísticas
con un alto grado de precisión.

Estos avances se deben, en gran medida, al entrenamiento sobre grandes colúmenes de datos y a la capacidad de estos modelos para capturar patrones estadísticos complejos en el lenguaje \cite{vaswani2017attention,brown2020language}.

No obstante, a pesar de su elevado rendimiento en tareas generales de generación de texto, estos modelos presentan limitaciones significativas cuando se enfrentan a problemas que requieren razonamiento estructurado. A diferencia
de los sistemas diseñados específicamente para la inferencia lógica o el cálculo simbólico, los \emph{LLMs} no siguen un proceso explícito de razonamiento, sino que generan respuestas basándose en la probabilidad de aparición de las
palabras en función del contexto previo. Este enfoque, aunque eficaz desde el punto de vista lingüístico, no garantiza que el modelo esté realizando un proceso de razonamiento correcto o
verificable \cite{bommasani2021opportunities}.

Esta limitación se hace especialmenente evidente en tareas que implican múltiples pasos intermedios, como la resolución de problemas matemáticos, la planificación o el análisis lógico. En este tipo de situaciones, los modelos pueden
producir respuestas que, aunque aparentemente coherentes, contienen errores en los pasos intermedios o en la conclusión final.

En la figura~\ref{fig:dosRespuestas} se ilustra este problema mediante un ejemplo sencillo: ante una misma pregunta matemática, un modelo puede ofrecer una respuesta directa incorrecta, mientras que un enfoque basado en razonamiento
paso a paso permite descomponer el problema en operaciones intermedias que conducen a la solución correcta. Este contraste pone de manifiesto que la generación de pasos explícitos no solo mejora la interpretabilidad del proceso,
sino que también incrementa la fiabilidad del resultado final.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{images/ComparativaEnfoques2.png}
    \caption{Ejemplo comparativo: respuesta directa errónea frente a razonamiento paso a paso. Fuente: elaboración propia}
    \label{fig:dosRespuestas}
\end{figure}


Además, los \emph{LLMs} tienden a generar una única respuesta por defecto, sin explorar alternativas ni considerar diferentes posibles soluciones. Esta característica contrasta con los enfoques clásicos de resolución de problemas,
en los que es habitual evaluar múltiples hipótesis antes de seleccionar la más adecuada. Como resultado, el modelo puede quedar limitado a una única trayectoria de generación, sin mecanismos internos que permiten revisar,
comparar o corregir sus propias decisiones.

En este contexto, surge la necesidad de desarrollar estrategias que permitan guiar el proceso de generación de los \emph{LLMs} hacia formas de razonamiento más estructuradas y fiables. Estas estrategias buscan introducir mecanismos
que fomenten la generación de pasos intermedios, la exploración de múltiples soluciones o la evaluación de las respuestas producidas. El estudio de estas técnicas constituye una línea de investigación activa, cuyo objetivo es
mejorar la capacidad de los modelos para abordar tareas complejas que van más allá de la simple generación de texto.

% Aquí no he mencionado anda de los problemas de índole lingüística (para los que va destinado este proyecto) ya que he querido hacer una introducción muy generica, sin meterme en nada en concreto.

%────────────────────── Chain-of-Thought (CoT) ──────────────────────────
\section{Chain-of-Thought (CoT)}

El concepto \emph{Chain-of-Thought (CoT)} surge como una de las primeras estrategias eficaces para mejorar la capacidad de razonamiento de los modelos de lenguaje en tareas complejas. Frente al enfoque tradicional, en el que el modelo
genera directamente una respuesta final, \emph{CoT} propone guiar el proceso de generación mediante la inclusión explícita de pasos intermedios que relfejen el razonamiento seguido para alcanzar dicha respuesta \cite{wei2022chain}.

La idea fundamental consiste en modificar la forma en que se formula la entrada al modelo, de modo que este produzca no solo el resultado, sino también una secuencia de inferencias o transformaciones que conducen a él. Este enfoque
puede implementarse, por ejemplo, mediante instrucciones en el \emph{prompt} que inviten al modelo a "pensar paso a paso", lo que conduce a la generación de estructuras de razonamiento más elaboradas.

Diversos trabajos han demostrado que esta simple modificación en la formulación de la tarea puede producir mejoras significativas en problemas que requieren múltiples pasos de inferencia, como los ejercicios matemáticos o las tareas
de lógica. En particular, se ha observado que los modelos son capaces de resolver correctamente problemas que anteriormente fallaban cuando se les proporciona un contexto que favorece la descomposición del problema en etapas
intermedias \cite{wei2022chain}.

Desde el punto de vista conceptual, \emph{CoT} puede interpretarse como una forma de hacer explícito un proceso que, en ausencia de este enfoque, permanece implícito dentro del modelo. Al externalizar los pasos intermedios, se facilita
tanto la interpretación del resultado como la posibilidad de analizar en qué punto se produce un error en caso de que la solución final sea incorrecta.

% Mencionamos también el tema de las limitaciones

Sin embargo, a pesar de sus ventajas, este enfoque presenta también limitaciones importantes. En primer lugar, el modelo sigue generando una única trayectoria de razonamiento, lo que implica que un error en alguno de los pasos intermedios
puede comprometer el resultado final sin posibilidad de corrección. Además, no existe un mecanismo interno que permita evaluar la calidad de los pasos generados ni comparar distintas alternativas de razonamiento.
% esta limitación desaparecerá cuando introduzcamos Beam Search

Por otro lado, la eficacia se \emph{CoT} depende en gran medida de la calidad del \emph{prompt} utilizado, así como de la capacidad del modelo para seguir instrucciones de forma consistente. En algunos casos, el modelo puede generar pasos
intermedios que resultan aceptables desde el punto de vista lingüístico, pero que no aportan un avance real en la resolución del problema.

En conjunto, \emph{Chain-of-Thought} representa un avance significativo en la forma de abordar el razonamiento con modelos de lenguaje, al introducir la idea de estructurar la generación mediante pasos intermedios. No obstante, sus
limitaciones ponen de manifiesto la necesidad de desarrollar enfoques más avanzados que permitan explorar múltiples trayectorias y evaluar su calidad de manera sistemática.

%────────────────────── Extensiones de razonamiento paso a paso ──────────────────────────
\section{Extensiones de Razonamiento Paso a Paso}

El enfoque  \emph{Chain-of-Thought (CoT)} supone un avance significativo en la mejora del razonamiento de los modelos de lenguaje, pero, como se ha señalado, presenta limitaciones importantes, especialmente en lo relativo a la
generación de una trayectoria de razonamiento. Esta restricción ha motivado el desarrollo de métodos que amplían este enfoque, permitiendo explorar múltiples posibles soluciones o integrar mecanismos adicionales de evalucación.

Una de las propuestas más destacadas es \emph{Self-Consistency}, que consiste en generar múltiples cadenas de razonamiento independientes a partir del mismo problema y seleccionar la respuesta final mediante un criterio de agregación,
como el voto mayoritario \cite{wang2022self}. Este enfoque permite reducir el impacto de errores en trayectorias individuales, aunque las distintas soluciones no interactúan entre sí y la evaluación se realiza únicamente sobre
el resulado final.

Otra línea de trabajo relevante es \emph{Tree of Thoughts (ToT)}, que extiende la idea de \emph{CoT} al modelar el razonamiento como una estructura en forma de árbol \cite{yao2023tot}. En este caso, el modelo genera diferentes
alternativas en cada paso, lo que permite explorar múltiples caminos y seleccionar aquellos que resultan más prometedores. Este enfoque mejora la capacidad de exploración, pero introduce un mayor coste computacional y requiere mecanismos
de evaluación más elaborados.

Por su parte, el método \emph{ReAct} combina el razonamiento con la ejecución de acciones externas dentro de un mismo proceso iterativo \cite{yao2023react}. Esto permite al modelo interactuar  con herramientas o fuentes de información
adicionales, lo que resulta especialmente útil en tareas donde el conocimiento no está completamente contenido en el propio modelo.

En conjunto, estas propuestas reflejan una evolución hacie enfoques que superan el razonamiento lineal, incorporando exploración, evaluación y, en algunos casos, interacción con el entorno. Esta línea de investigación resulta
especialmente relevante en el desarrollo de sistemas capaces de abordar problemas complejos de forma más robusta.



%────────────────────── Métodos de búsqueda en generación de secuencias ──────────────────────────
\section{Métodos de Búsqueda en Generación de Secuencias}

El proceso de generación de texto en los modelos de lenguaje puede entenderse como un problema de búsqueda en el espacio de todas las posibles secuencias de palabras. En cada paso, el modelo asigna una probabilidad a los posibles
siguientes \emph{tokens} en función del contexto previo, para que sea el método de búsqueda el que posteriormente determina cómo se selecciona la continuación de la secuencia generada.

El enfoque más sencillo es la estrategia \emph{greedy}, que consiste en seleccionar en cada paso el token con mayor probabilidad. Este método es eficiente y produce resultados coherentes en muchos casos, pero presenta una limitación
importante: al tomar decisiones de forma local, puede descartar opciones que, aunque inicialmente menos probables, podrían conducir a mejores resultados a largo plazo \cite{jurafsky2009speech}.

Como se muestra en la figura~\ref{fig:GreedySearch}, este comportamiento implica que en cada paso únicamente se sigue la alternativa más probable, ignorando otras posibles continuaciones que quedan descartadas desde etapas tempranas del proceso.
Esta característica explica por qué el método puede converger rápidamente a soluciones subóptimas.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/GreedySearch.png}
    \caption{Ilustración del algoritmo de búsqueda voraz en la generación de texto. Fuente: elaboración propia}
    \label{fig:GreedySearch}
\end{figure}

Para introducir mayor diversidad en la generación, se utilizan métodos de muestreo \emph{(sampling)}, en los que el siguiente \emph{token} se selecciona de manera probabilística en lugar de determinista. Entre las variantes más comunes
se encuentran:

\begin{itemize}
  \item \emph{Top-k sampling}, que restringe la selección a los $k$ \emph{tokens} más probables.
  \item \emph{Top-p sampling} o \emph{nucleus sampling}, que considera el conjunto mínimo de \emph{tokens} cuya probabilidad acumulada supera un determinado umbral \cite{holtzman2020curious}.
\end{itemize}

Estas estrategias permiten generar salidas más variadas, aunque pueden introducir incoherencias si no se controlan adecuadamente.

Otro enfoque consiste en mantener varias posibles secuencias en paralelo durante el proceso de generación. En lugar de construir una única solución paso a paso, se exploran múltiples alternativas de forma simultánea, evaluando su calidad
en cada iteración. Este tipo de métodos permite equilibrar la exploración y la explotación, reduciendo así el riesgo de quedar atrapado en decisiones subóptimas tomadas en etapas tempranas.

Tal y como se ilustra en la figura~\ref{fig:HipotesisEnParalelo}, este tipo de estrategias permite ramificar el proceso de generación desde un mismo estado inicial, dando lugar a diferentes trayectorias que pueden evolucionar de forma independiente.
Este enfoque resulta especialmente útil para evitar decisiones prematuras y favorecer la exploración del espacio de soluciones.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/HipotesisEnParalelo.png}
    \caption{Estrategia de exploración de alternativas mediante generación paralela de secuencias. Fuente: elaboración propia}
    \label{fig:HipotesisEnParalelo}
\end{figure}

En conjunto, los distintos métodos de búsqueda reflejan un compromiso entre eficiencia, diversidad y calidad de las secuencias generadas. Mientras que estrategias como \emph{greedy} priorizan la rapidez, los métodos basados en muestreo
introducen variabilidad, y los enfoques que mantienen múltiples hipótesis permiten una exploración más amplia del espacio de soluciones. Esta última idea resulta especialmente relevante en el contexto del razonamiento, donde la posibilidad
de considerar distintas alternativas puede mejorar significativamente la calidad de la respuesta final.


%────────────────────── Beam Search en detalle ──────────────────────────
\section{Beam Search en Detalle}

Entre los distintos métodos de búsqueda utilizados en la generación de secuencias, el algoritmo de \emph{Beam Search} ocupa un lugar destacado debido a su capacidad para equilibrar la exploración de múltiples alternativas con un coste
computacional controlado. A diferencia de estrategias como \emph{greddy search} (búsqueda voraz), que siguen una única trayectoria, o los métodos de muestreo, que introducen aleatoriedad, \emph{Beam Search} mantiene de forma simultánea un
conjunto limitado de hipótesis candidatas durante el proceso de generación.

El funcionamiento del algoritmo se basa en la idea de conservar, en cada paso, las $k$ secuencias parciales más prometedoras según una función de puntuación. Este parámetro, conocido como \emph{beam width}, determina el número de
trayectorias que se mantienen activas en cada iteración. A partir de estas secuencias, el modelo genera posibles continuaciones, que son evaluadas y ordenadas para seleccionar nuevamente las $k$ mejores. Este proceso se repite de forma
iterativa hasta alcanzar una condición de finalización, como la generación de una secuencia completa o un numero máximo de pasos \cite{jurafsky2009speech}.

Desde un punto de vista conceptual, \emph{Beam Search} puede interpretarse como una exploración controlada del espacio de soluciones. En lugar de comprometerse con una única trayectoria desde el inicio, el algoritmo permite reconsiderar
decisiones a medida que se genera la secuencia, manteniedno abiertas varias alternativas en paralelo. Esto reduce la probabilidad de descartar prematuramente opciones que podrían resultar óptimos en etapas posteriores (como sucede en la
búsqueda voraz).

La figura~\ref{fig:BeamSearchImagen2} ilustra este proceso mediante un árbol de generación en el que, a partir de un estado inicial, se exploran múltiples continuaciones posibles. En cada iteración, únicamente se conservan las $k$ trayectorias
mejor valoradas, mientras que el resto son descartadas, lo que permite limitar el crecimiento exponencial del número de secuencias. Este mecanismo de expansión y poda sucesiva constituye el núcleo del algoritmo y explica su capacidad para
combinar exploración y eficiencia.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.95\textwidth]{images/BeamSearchImagen2.png}
    \caption{Selección iteratica de las $k$ mejores secuencias en \emph{Beam Search}. Fuente: elaboración propia}
    \label{fig:BeamSearchImagen2}
\end{figure}

No obstante, la eficacia del algoritmo depende en gran medida de la función de puntuación utilizada para evaluar las secuencias candidatas. En aplicaciones tradicionales de procesamiento del lenguaje, esta puntuaciñon suele basarse en
la probabilidad acumulada de la secuencia generada. Sin embargo, este criterio no siempre es suficiente en tareas que requieren razonamiento, donde la calidad de una solución no depende únicamente de su probabilidad, sino también de la
coherencia y validez de los pasos intermedios.

Otra limitación relevante es que \emph{Beam Search} sigue siendo un método determinista en su forma básica, lo que implica que puede generar resultados similares en ejecuciones repetidas si no se introduce algún mecanismo de variabilidad.
Además, el valor del parámetro $k$ unfluye directamente en el equilibrio entre coste computacional y calidad de las soluciones: valores pequeños pueden limitar la exploración, mientras que valores grandes incrementan significativamente
el tiempo de cálculo.

A pesar de estas limitaciones, \emph{Beam Search} constituye una herramienta fundamental en la generación de secuencias, y su capacidad para mantener múltiples hipótesis lo convierte en un candidato natural para su aplicación en tareas de
razonamiento con modelos de lenguaje. En particular, su estructura iterativa y su enfoque basado en la selección de trayectorias prometedoras resultan especialmente adecuados para integrar mecanismos adicionales de evaluación, lo que
permite extender su uso más allá de los contextos tradicionales.


%────────────────────── Uso de Beam Search en razonamiento con LLMs ──────────────────────────
\section{Uso de Beam Search en Razonamiento con LLMs}

Aunque el algoritmo de \emph{Beam Search} ha sido tradicionalmente utilizado en tareas como la traducción automática o la generación de texto, su estructura resulta especialmente adecuada para abordar problemas de razonamiento en modelos de
lenguaje. En este contexto, el objetivo no es únicamente generar una secuencia probable, sino explorar distintas posibles trayectorias de razonamiento y seleccionar aquellas que conducen a soluciones más adecuadas.

La combinación de \emph{Beam Search} con enfoques como \emph{Chain-of-Thought} permite reinterpretar el proceso de generación como una búsqueda sobre posibles cadenas de razonamiento. En lugar de producir una única secuencia de pasos intermedios,
el modelo puede generar múltiples alternativas en cada etapa, dando lugar a diferentes hipótesis que desarrollan en paralelo. Este enfoque permite superar uan de las principales limitaciones del razonamiento lineal, al incorporar la posibilidad
de reconsiderar decisiones y explorar distintos caminos hacia la solución.

Como se muestra en la figura~\ref{fig:BeamSearchYCoT}, esta diferencia puede entenderse como el paso de un proceso de generación basado en una única trayectoria (\emph{Chain-of-Thought}) a otro en el que se mantienen múltiples trayectorias de
razonamiento de forma simultánea (\emph{Beam Search} + \emph{CoT}). Esta ampliación del espacio de búsqueda permite aumentar la probabilidad de encontrar soluciones correctas, especialmente en problemas que requieren varios pasos intermedios.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.95\textwidth]{images/BeamSearchYCoT.png}
    \caption{Estrategias de generación: trayectoria única frente a múltiples trayectorias. Fuente: elaboración propia}
    \label{fig:BeamSearchYCoT}
\end{figure}

En este escenario, cada trayectoria mantenida por el algoritmo puede interpretarse como una posible solución parcial, que se va refinando a medida que avanza el proceso de generación. La aplicación de un mecanismo de selección en cada iteración
permite priorizar aquellas trayectorias que presentan mejores propiedades, ya sea en términos de coherencia, utilidad o proximidad a la solución final \cite{yao2023tot}.

No obstante, la adaptación de \emph{Beam Search} al razonamiento con \emph{LLMs} plantea desafíos adicionales respecto a su uso tradicional. En primer lugar, la función de puntuación utilizada para comparar las distintas trayectorias resulta
crítica. Mientras que en tareas de generación de texto esta puntuación suele basarse en la probabilidad acumulada de la secuencia, en problemas de razonamiento es necesario considerar también la calidad de los pasos intermedios. Esto implica
la necesidad de definir criterios más complejos que permitan evaluar no solo el resultado final, sino también el proceso seguido hasta alcanzarlo.

Además, el uso de \emph{Beam Search} en este contexto requiere integrar mecanismos que permitan generar pasos de razonamiento coherentes y relevantes en cada iteración. Esto puede lograrse mediante el uso de técnicas de \emph{propmting}
adecuadas, que orienten al modelo hacia la producción de pasos intermedios bien estructurados, o mediante la incorporación de componentes adicionales encargados de guiar el proceso de generación.

Por último, cabe destacar que la aplicación de \emph{Beam Search} al razonamiento introduce un equilibrio delicado entre exploración y coste computacional. A medida que se incrementa el número de traycetorias consideradas, mejora la capacidad
del sistema para encontrar soluciones de mayor calidad, pero también aumenta el número de evaluaciones necesarios. Por tanto, resulta necesario ajustar cuidadosamente los parámetros del algoritmo para lograr un compromisoadecuado entre
eficacia y rendimiento.

En conjunto, la integración de \emph{Beam Search} en el proceso de razonamiento con modelos de lenguaje representa un paso hacia enfoques más estructurados y controlados, en los que la generación de soluciones no depende de una única trayectoria,
sino de la exploración y selección de múltiples alternativas. Esta idea constituye la base sobre la que se desarrrollan sistemas más avanzados capaces de combinar generación, evaluación y toma de decisiones dentro de un mismo marco de trabajo.


%────────────────────── Evaluación del razonamiento en LLMs ──────────────────────────
\section{Evaluación del Razonamiento en LLMs}

Uno de los aspectos más complejos en el uso de modelos para tareas de razonamiento es la evaluación de la calidad de las soluciones generadas. A diferencia de otros problemas de procesamiento del lenguaje natural, donde es posible comparar
directamente la salida del modelo con una respuesta de referencia, en tareas de razonamiento resulta necesario considerar no solo el resultado final, sino también el proceso seguido para alcanzarlo.

En enfoques tradicionales, la evaluación suele centrarse en la corrección de la respuesta final mediante métricas como la exactitud o el \emph{exact match}. Este tipo de medidas resulta adecuado en tareas cerradas, como la resolución de problemas
matemáticos con una única solución correcta \cite{cobbe2021training}. Sin embargo, presentan limitaciones importantes en escenarios donde el razonamiento intermedio juega un papel fundamental o donde pueden existir múltiples soluciones plausibles.

En este contexto, ha surgido un interés creciente por evaluar explícitamente las traycetorias de razonamiento generadas por los modelos. Este enfoque implica analizar la coherencia, consistencia y utilidad de los pasos intermedios, en lugar de
limitarse a verificar el resultado final. La evaluación del proceso permite identificar errores que no serían detectables mediante métricas tradicionales, especialmente en casos en los que una respuesta correcta puede haberse obtenido a partir
de un razonamiento incorrecto o incompleto.

Una de las aproximaciones más recientes consiste en utilizar modelos de lenguaje como evaluadores de las soluciones generadas por otros modelos o por el propio sistema en distintas etapas del proceso. Este enfoque, conocido como
\emph{LLM-as-a-judge}, se basa en la capacidad de los modelos para analizar y valorar la calidad de una respuesta, proporcionando tanto una puntuación como una justificación textual \cite{zheng2023judging}.

%\begin{figure}[h]
%    \centering
%    \includegraphics[width=0.95\textwidth]{images/EvaluaciónSoluciones.png}
%    \caption{Evaluación de trayectorias de razonamiento mediante modelos de lenguaje. Fuente: elaboración propia}
%    \label{fig:EvaluaciónSoluciones}
%\end{figure}

En este tipo de esquemas, la generación y la evaluación se conciben como procesos diferenciados pero complementarios, lo que permite introducir mecanismos más flexibles para comparar distintas alternativas y seleccionar aquellas que resultan más
adecuadas en función de criterios definidos.

El uso de modelos de lenguaje como evaluadores presenta varias ventajas. Por un lado, permite considerar aspectos cualitativos del razonamiento, como la claridad o la coherencia, que resultan difíciles de capturar mediante métricas automáticas
tradicionales. Por otro, facilita la integración de la evaluación dentro del propio proceso de generación, abriendo la puerta a sistemas en los que las soluciones no solo se generan, sino que también se analizan y refinan de manera iterativa.

No obstante, este enfoque trambién plantea desafíos importantes. La evalucación realizada por un modelo puede estar sujeta a sesgos, inconsistencias o falta de precisión, especialmente cuando se trata de tareas complejas o ambiguas. Además,
el uso de modelos adicionales para evaluar las soluciones incrementa el coste computacional del sistema, lo que puede limitar su aplicabilidad en ciertos contextos.

En conjunto, la evaluación del razonamiento se configura como un elemento clave en el desarrollo de sistemas basados en modelos de lenguaje. La capacidad de analizar no solo el resultado final, sino también el proceso seguido para alcanzarlo.
resulta fundamental para avanzar hacia enfoques más robustos, en los que la generación y la evaluación se integren dentro de un mismo marco de trabajo.



%────────────────────── Limitaciones del estado del arte ──────────────────────────
\section{Limitaciones del Estado del Arte}

A pesar de los avances recientes en el desarrollo de técnicas para mejorar el razonamiento en modelos de lenguaje, las soluciones existentes presentan limitaciones que dificultan su aplicación en escenarios más complejos o exigentes. Estas
limitaciones no solo afectan al rendimiento de los modelos, sino también a la forma en que se estructuran los procesos de generación y toma de decisiones.

En el caso de \emph{Chain-of-Thought}, aunque se ha demostrado que la generación de pasos intermedios mejora significativamente el rendimiento en tareas de razonamiento, el enfoque sigue dependiendo de una única trayectoria. Esto implica que un
error en cualquier punto del proceso puede propagarse hasta la respuesta final sin posibilidad de corrección. Además, no se dispone de mecanismos internos que permitan evaluar la calidad de los pasos generados o reconsiderar decisiones previas
\cite{wei2022chain}.

Las extensiones basadas en la generación de múltiples trayectorias, como \emph{Self-Consistency}, introducen una mejora al considerar varias soluciones posibles. Sin embargo, estas trayectorias se generan de forma independiente y la selección final
se basa únicamente en el resultado, sin tener en cuenta la calidad del razonamiento intermedio. Esto limita su capacidad para discriminar entre soluciones que pueden parecer correctas superficialmente pero que se basan en procesos
inconsistentes \cite{wang2022self}.

Asimismo, la exploración del espacio de soluciones suele implicar un compromiso complejo entre calidad y coste computacional. Métodos como \emph{Tree of Thoughts}, que permiten considerar múltiples alternativas tienden a requerir un mayor
número de evaluaciones, lo que dificulta su aplicación en entornos donde lso recursos son limitados.

Por último, muchos de los enfoques existentes carecen de un marco unificado que integre de manera coherente la generación de soluciones, su evaluación y el control del flujo de razonamiento. En lugar de ello, estos componentes suelen abordarse
de forma aislada, lo que reduce la capacidad del sistema para gestionar el proceso de forma estructurada.

Estas limitaciones se resumen de forma esquemática en la figura~\ref{fig:LimitacionesMetodosRazonamiento2}, donde se comparan algunos de los principales enfoques y sus restricciones más relevantes. Esta visión global permite identificar de manera
clara los aspectos que aún no están resueltos, especialmente en lo relativo a la evaluación intermedia de las trayectorias y al control eficiente del proceso de búsqueda.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.95\textwidth]{images/LimitacionesMetodosRazonamiento2.png}
    \caption{Comparativa de limitaciones en estrategias de razonamiento automático. Fuente: elaboración propia}
    \label{fig:LimitacionesMetodosRazonamiento2}
\end{figure}

En conjunto, estas limitaciones ponen de manifiesto la necesidad de desarrollar enfoques que permitan combinar de manera efectiva la exploración de múltiples trayectorias, la evaluación de su calidad y el control iterativo del proceso de
razonamiento. Esta necesidad constituye el punto de partida para el enfoque propuesto en este trabajo.

%────────────────────── Imaging model and objectives ──────────────────────────
%\section{Imaging model and objectives}

%Assume \(y = A x + n\). The SR network \(f_\theta\) estimates
%\(\hat x = f_\theta(y)\).
%We use the composite objective
%\begin{equation}
%  \mathcal{L}_{\mathrm{SR}}
%  = \lambda_1 \lVert \hat x - x \rVert_1
%  + \lambda_s \left(1-\operatorname{SSIM}(\hat x, x)\right)
%  + \lambda_{\mathrm{tv}} \operatorname{TV}(\hat x).
%  \label{eq:sr_loss}
%\end{equation}
%For segmentation logits \(z = g_\phi(\hat x)\) and mask \(m\),
%\begin{equation}
%  \mathcal{L}_{\mathrm{seg}}
%  = 1 - \operatorname{Dice}\!\left(\sigma(z), m\right)
%    + \beta\, \operatorname{CE}\!\left(\sigma(z), m\right).
%  \label{eq:seg_loss}
%\end{equation}
%Total loss:
%\(\mathcal{L} = \mathcal{L}_{\mathrm{SR}} + \gamma\,\mathcal{L}_{\mathrm{seg}}\).


%\section{Metrics}
%Performance metrics
%\begin{align}
%\operatorname{PSNR}(x,\hat x) &= 10\log_{10}\!\frac{L^2}{\operatorname{MSE}(x,\hat x)},\\
%\operatorname{SSIM}(x,\hat x) &= \frac{(2\mu_x\mu_{\hat x}+C_1)(2\sigma_{x\hat x}+C_2)}
%{(\mu_x^2+\mu_{\hat x}^2+C_1)(\sigma_x^2+\sigma_{\hat x}^2+C_2)},\\
%\operatorname{Dice}(p,m) &= \frac{2\sum p m}{\sum p + \sum m}.
%\end{align}

%────────────────────── Chapter 3 — LangGraph ────────────────────────────
\chapter{LangGraph}

%────────────────────── Introducción a LangGraph ──────────────────────────
\section{Introducción a LangGraph}

En los sistemas basados en modelos de lenguaje, el enfoque tradicional ha consistido en realizar llamadas independientes y aisladas a un modelo, evolucionando posteriormente
hacia sistemas más complejos capaces de gestionar flujos de ejecución estructurados. Esto cambio responde así a la necesidad de construir aplicaciones que no solo generen
respuestas, sino que integren múltiples pasos de procesamiento, toma de decisiones y manejo de información intermedia.

En este contexto surge \emph{LangGraph}, una herramienta que se presenta como alternativa a los enfoques lineales, y diseñada para facilitar la construcción de agentes
mediante la definición de flujos de ejecución basados en grafos. \emph{LangGraph} modela procesos de forma más flexible, en los que pueden existir bifurcaciones, ciclos y
dependencias entre distintos pasos. \cite{langgraph2024}

A diferencia de un flujo secuencial, un grafo permite mayor flexibilidad, al representar de manera más natural procesos complejos, en los que el sistema puede iterar,
tomar decisiones en función del estado actual o repetir determinadas operaciones hasta alcanzar un resultado adecuado. Este tipo de estructura es clave en sistemas que requieren
un comportamiento dinámico, como es el caso de agentes con capacidad de razonamiento iterativo.

Esta diferencia se ilustra en la figura~\ref{fig:ComparaciónEnfoques}, donde se comparan ambos paradigmas: mientras que el enfoque tradicional sigue un flujo lineal de ejecución,
el modelo basado en grafos permite representar múltiples caminos, iteraciones y dependencias entre los distintos componentes del sistema, lo que facilita la construcción de
procesos más complejos y adaptativos.

\begin{figure}[h]
    \centering

    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=0.8\linewidth]{images/DiagramaFlujoSecuencial.png}
        \caption{Ejemplo de enfoque tradicional mediante diagrama de flujo lineal}
    \end{subfigure}
    \hfill
    \begin{subfigure}{0.45\textwidth}
        \centering
        \includegraphics[width=\linewidth]{images/DiagramaFlujoGrafo2.png}
        \caption{Ejemplo de enfoque moderno mediante diagrama de flujo en grafo}
    \end{subfigure}

    \caption{Comparación entre enfoques. Fuente: Elaboración propia}
    \label{fig:ComparaciónEnfoques}
\end{figure}

Además, la modularidad que ofrece \emph{LangGraph} al facilitar la separación de responsabilidades dentro del sistema, permitiendo definir distintos componentes que se encargan de
tareas específicas no solo mejorar la claridad del diseño, sino que a su vez favorece la reutilización y extensión del sistema. Algunas de las tareas específicas ejecutadas por los
compenentes son la generación de contenido, la evaluación de resultados o la actualización del estado interno.

De este modo, \emph{LangGraph} constituye una base sólida para el desarrollo de agentes avanzados, proporcionando un marco de trabajo que permite superar las limitaciones de los
enfoques secuenciales tradicionales y ofreciendo una forma más estructurada y flexible de diseñar agentes basados en modelos de lenguaje.

%\begin{algorithm}
%\caption{Joint SR+Seg training}
%\label{alg:train}
%\begin{algorithmic}[1]
%\Procedure{Train}{$\mathcal{D}$, $\theta$, $\phi$}
%  \For{epoch $=1..E$}
%    \For{batch $(y,x,m)\sim \mathcal{D}$}
%      \State $\hat x \gets f_\theta(y)$
%      \State $z \gets g_\phi(\hat x)$; $p\gets\sigma(z)$
%      \State $\mathcal{L}\gets \mathcal{L}_{\mathrm{SR}}(\hat x,x)+\gamma\,\mathcal{L}_{\mathrm{seg}}(p,m)$
%      \State Update $(\theta,\phi)$ by Adam on $\nabla \mathcal{L}$
%    \EndFor
%  \EndFor
%\EndProcedure
%\end{algorithmic}
%\end{algorithm}

%────────────────────── Concepto de grafos aplicado a agentes ──────────────────────────
\section{Concepto de Grafo Aplicado a Agentes}

Los grafos han sido y son ampliamente utilizados en diferentes ámbitos de la informática para modelar sistemas complejos, ya que permite
representar de forma clara las conexiones y dependencias entre los diferentes componentes. Su estructura matemática consta de un conjunto de elementos,
denominados nodos, conectados entre sí mediante relaciones llamadas aristas. \cite{cormen2009algorithms}

En el contexto de los agentes basados en modelos de lenguage, los grafos ofrecen una forma natural de estructurar el flujo de ejecución. Dentro de este
contexto, los nodos representan etapas del proceso, mientras que las transiciones entre ellos definen el flujo de ejecución del sistema. En otras palabras,
cada nodo corresponde a una operación específica, mientras que las aristas determinan cómo se transita entre ellas.

Este enfoque permite superar ciertas limitaciones de los sistemas lineales tradicionales, en los que la ejecución sigue un único camino predefinido. Además,
los grafos facilitan la representación de procesos iterativos, en los que el sistema puede volver a ejecutar determinadas partes del flujo hasta cumplir una
condición concreta. Esta característica resulta especialmente relevante en el diseño de agentes con capacidad de razonamiento, donde es habitual generar, evaluar
y refinar soluciones de manera progresiva. \cite{bondy1976graph}

Otra ventaja a tener en cuenta con el uso de grafos es su interpretabilidad, pues su capacidad para reflejar de forma explícita la estructura
del sistema facilita actividades como el análisis del comportamiento del agente y la identificación de posibles puntos de mejora.

En consecuencia, el uso de grafos en la construcción de agentes permite diseñar sistemas más modulares, flexibles y adaptables, facilitando la implementación
de flujos de ejecución complejos que serían difíciles de representar mediante enfoques puramente secuenciales.


%────────────────────── Conceptos fundamentales de LangGraph ──────────────────────────
\section{Conceptos Fundamentales de LangGraph}

Para comprender el funcionamiento de \emph{LangGraph}, es necesario analizar los elementos básicos que conforman su modelo de ejecución.
En conjunto, estos elementos proporcionan una base para el diseño y construcción de agentes complejos, permitiendo combinar generación, evaluación y toma
de decisiones dentro de un mismo sistema:

\subsection{Estado}

Un estado representa la información que se mantiene a lo largo de la ejecución del sistema. Este estado actúa como un contenedor de datos, que permite almacenar
información relevante a medida que el agente progresa, logrando así que cada paso ejecutado tenga acceso al contexto generado en las etapas anteriores.
De este modo, se facilita la construcción de aquellos procesos que dependen del historial o de resultados intermedios.

\subsection{Nodo}

Un nodo es un punto de intersección, unidad básica de procesamiento dentro del grafo. En esencia, actúa como un punto de referencia que permite organizar datos de
manera ordenada y eficiente. Cada nodo se encarga de ejecutar una operación concreta, como puede ser la generación de contenido, la evaluación de una respuesta o la
transformación de datos almacenados en el estado. Esta separación en nodos permite estructurar el sistema de forma modular, asignando responsabilidades específicas
a cada componente. Además, cuanto más rica y conectada sea la red de nodos, mayor es la capacidad del sistema para interpretar y generar conocimiento. \cite{nodes_msmk2025}

\subsection{Arista}

Las aristas o transiciones definen cómo se produce el paso de un nodo a otro. Estas transiciones pueden ser de dos tipos:
\begin{itemize}
  \item De tipo fijo, siguiendo una secuencia predeterminada.
  \item De tipo condicional, en función de determinados criterios evaluados durante la ejecución.
\end{itemize}
El uso de aristas introduce lógica de decisión dentro del flujo, adaptando el comportamiento del sistema según la información disponible en cada momento. \cite{aperezl_langgraph2024}

\subsection{Bucles}

La presencia de bucles dentro del grafo facilita la implementación de procesos iterativos gracias a que, a diferencia de los enfoques lineales,
\emph{LangGraph} permite que la ejecución vuelva a nodos previamente visitados. Esta característica resulta especialmente útil en sistemas que requieren
refinamiento progresivo, como los basados en razonamiento paso a paso.

\subsection{Puntos de Inicio y Finalización}

Finalmente, es necesario considerar la existencia de puntos de inicio y finalización dentro del grafo. El nodo inicial marca el punto de salida, desde donde
da comienzo la ejecución, mientras que las condiciones de finalización controlan cuándo se ha alcanzado un resultado satisfactorio y, por lo tanto,
detener el proceso. Este mecanismo permite controlar el flujo de ejecución y evitar iteraciones innecesarios.


%────────────────────── Conceptos fundamentales de LangGraph ──────────────────────────
\section{Funcionamiento General de LangGraph}

Una vez definidos los elementos fundamentales de \emph{LangGraph}, resulta necesario comprender cómo interactuan entre sí durante la ejecución de un agente. El funcionamiento de
\emph{LangGraph} puede entenderse como la evaluación progresiva de un grafo, donde el sistema transita entre los diferentes nodos siguiendo las transiciones definidas (arsitas) y
actualizando el estado en cada paso.

La ejecución comienza a partir de una configuración inicial, que contiene la información de partida necesaria para la ejecución. A partir de este punto,
se accede al nodo inicial del grafo, que actúa a su vez como punto de entrada del sistema. La modificación de estado producida por la operación ejecutada por el nodo inicial será
utilizada en las etapas posteriores.

Tras la ejecución de un nodo, el sistema evalúa todas las posibles transiciones disponibles en dicho instante.
Dependiendo de la configuración del grafo, la siguiente etapa puede:

\begin{itemize}
  \item Estar determinada de forma fija.
  \item Depender de condiciones evaluadas a partir del estado actual.
\end{itemize}

Con dicho condicionante, el mecanismo logra introducir dinamismo en el flujo de ejecución, permitiendo que el agente sea capaz de adaptarse a la información generada en tiempo
de ejecución, y adoptar el comportamiento pertinente.

A medida que el proceso avanza, el estado de va actualizando de forma incremental, acumulando a su vez información relevante que podría influir en decisiones y transiciones futuras.
Este ciclo de ejecución de nodo, actualización de estado y transición se repite iterativamente hasta que se alcanza la condición de parada, previamente establecida. \cite{anderfernandez_langgraph2024}

En aquellos casos en los que el grafo incluye bucles, el sistema puede volver a nodos previamente ejecutados, lo que permite implementar procesos iterativos.
Este tipo de comportamiento resulta especialmente útil en escenarios en los que, antes de obtener un resultado final, es necesario refinar progresivamente una solución o evaluar
ditintas alternativas.

La ejecución finaliza cuando se cumple una condición de parada \cite{nocedal2006numerical}, obteniéndose un resultado generado a partir del estado acumulado. De esta forma, este enfoque permite
construir sistemas en los que la respuesta final es el resultado de múltiples pasos intermedios, en lugar de una única operación directa.


%────────────────────── Gestión del estado en LangGraph ──────────────────────────
\section{Gestión del Estado en LangGraph}

Uno de los elementos clave en el funcionamiento de sistemas basados en grafos es la gestión del estado, entendido como el conjunto de información que se mantiene y se actualiza a lo
largo de la ejecución. En el caso de \emph{LangGraph}, el estado desempeña un papel fundamental, ya que actúa como el medio a través del cual los distintos nodos comparten información
y coordinan su comportamiento.

A diferencia de enfoques más simples, en los que cada operación se ejecuta de forma aislada, el uso de un estado compartido permite construir procesos en los que cada paso
depende de los resultados obtenidos previamente. De este modo, el sistema es capaz de mantener un contexto acumulativo, lo que resulta especialmente útil en aplicaciones que requieren
múltiples etapas de procesamiento.

El estado puede contener distintos tipos de información, dependiendo del diseño del sistema. Entre los elementos más habituales se encuentran el historial de interacciones y los resultados
intermedios generados durante la ejecución, así como métricas o puntuaciones asociadas a diferentes alternativas. Esta flexibilidad permite adaptar el estado a las necesidades específicas de
cada aplicación.

Tal y como se representa en la figura~\ref{fig:EstadoMemoria}, estos elementos pueden integrarse dentro de una misma estructura de datos que actúa como memoria compartida del sistema.
Esta representación permite visualizar cómo el estado no solo almacena información pasada, sino que también incorpora resultados actuales y métricas que influyen directamente en la toma de decisiones.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/EstadoMemoria.png}
    \caption{Esquema del estado del sistema que incluye historial, resultados y puntuaciones. Fuente: elaboración propia}
    \label{fig:EstadoMemoria}
\end{figure}

Durante la ejecución del grafo, cada nodo tiene la capacidad de acceder al estado, modificarlo y añadir nueva información. Este proceso de actualización progresiva es lo que permite que el
sistema evolucione a lo largo del tiempo, incorporando nuevos datos que influyen en las decisiones posteriores.

No obstante, una gestión inadecuada del estado puede introducir problemas en el sistema. Por ejemplo, un crecimiento excesivo de la información almacenada puede afectar al rendimiento, mientras
que la inclusión de datos irrelevantes puede dificultar la toma de decisiones. Por ello, resulta necesario diseñar cuidadosamente qué información se almacena y cómo se actualiza en cada etapa
del proceso. \cite{weng2023llm_agents}

En conclusión, la gestión del estado no solo constituye un elemento técnico dentro de \emph{LangGraph}, sino que también representa un aspecto clave en el diseño de agentes basados en modelos
de lenguaje. Un estado bien defeinido permite mejorar la coherencia del sistema, facilitar el seguimiento del razonamiento y optimizar el proceso de toma de decisiones.


%────────────────────── Máquina de estados finitos aplicada a LangGraph ──────────────────────────
\section{Máquina de Estados Finitos Aplicada a LangGraph}
Las máquinas de estados finitos constituyen un modelo ampliamente utilizado para describir sistemas cuyo comportamiento puede dividirse en un conjunto de etapas bien definidas, entre las que se
producen transiciones en función de determinadas condiciones. Este enfoque resulta especialmente útil para modelar procesos en los que el flujo de ejecución no es estrictamente lineal, sino
que depende de decisiones tomadas durante la ejecución. \cite{hopcroft2001automata}

En el contexto de \emph{LangGraph}, este modelo permite interpretar el flujo del agente como una secuencia de estados conectados mediante transiciones. Cada nodo del grafo representa una etapa concreta
del proceso, mientras que las aristas determinan las posibles evoluciones del sistema en función de las condiciones evaluadas.

Como se indica en reiteradas ocasiones de este proyecto, a diferencia de los enfoques secuenciales tradicionales, el uso de una máquina de estados permite definir comportamientos más complejos, como
la posibilidad de reintentar una operación en caso de fallo, retroceder a una etapa anterior o seguir distintos caminos en función del resultado obtenido en un paso intermedio. Este tipo de control
resulta difícil de implementar mediante estructuras lineales, pero se adapta de forma natural a un modelo basado en estados.

En relación con este trabajo, este enfoque resulta especialmente relevante para estructurar el proceso de razonamiento del agente. En lugar de considerar el sistema como una única secuencia de pasos,
es posible dividirlo en distintas fases, como la generación de una solución, su evaluación y la toma de decisiones posteriores. Las transiciones entre estas fases permiten determinar si el proceso debe
continuar, repetirse o finalizar. \cite{harel1987statecharts}

Este modelo aporta, por tanto, una forma clara de organizar el flujo de ejecución, facilitando la implementación de comportamientos dinámicos y adaptativos. Además, permite representar de manera explícita
las posibles rutas de ejecución, lo que contribuye a una mejor comprensión del sistema y a una mayor facilidad para su análisis y depuración.


%────────────────────── Ejemplo de patrón ReAct en LangGraph ──────────────────────────
\section{Ejemplo de Patrón ReAct en LangGraph}

Una de las formas más representativas de utilizar \emph{LangGraph} en el diseño de agentes es mediante la implementación de patrones que combinan razonamiento y acción de manera iterativa. Este tipo de
enfoque permite que el sistema no solo genere una respuesta, sino que también interactúe con su entorno y utilice información externa para mejorar sus decisiones. \cite{yao2023react}

En este sentido, resulta especialmente ilustrativo considerar un esquema en el que el agente analiza una consulta inicial y decide si es necesario realizar alguna acción adicional antes de generar una
respuesta definitiva. Este proceso puede modelarse mediante un grafo en el que cada nodo representa una fase diferencia del comportamiento del agente.

En una primera etapa el sistema procesa la entrada del usuario y genera una interpretación inicial del problema. A partir de esta información, el agente puede decidir si dispone de suficiente conocimiento
para responder directamente o si, por el contrario, necesita apoyarse en alguna herramienta externa, como una función de \emph{Python} o una consulta a una API.

Esta decisión se traduce en una transición condicional dentro del grafo. Si el agente determina que no es necesaria ninguna acción adicional, el flujo de ejecución finaliza y se devuelve una respuesta. En
caso contrario, el sistema transita hacia un nodo encargado de ejecutar la acción correspondiente, obteniendo nueva información que se incorpora al estado.

Una vez se realizada la acción, el flujo no concluye, sino que vuelve al nodo inicial de razonamiento. De este modo, el agente puede reconsiderar la situación a la luz de los nuevos datos obtenidos, generando
un proceso iteerativo en el que se alternan fases de análisis y acción hasta alcanzar una solución adecuada.

Este comportamiento cíclico se representa de forma esquemática en la figura~\ref{fig:CicloRazonamientoAcción}, donde se observa la interacción bidireccional entre los nodos de razonamiento y acción, así como la
posibilidad de repetir la ejecución de acciones cuando sea necesario. Esta estructura refleja el carácter dinámico del agente y su capacidad para adaptarse progresivamente a la información disponible.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/CicloRazonamientoAcción.png}
    \caption{Esquema de interacción iterativa entre las fases de razonamiento y acción en un agente basado en grafos. Fuente: elaboración propia}
    \label{fig:CicloRazonamientoAcción}
\end{figure}

Este tipo de patrón resulta especialmenente útil en problemas donde la información es insuficiente o donde es necesario realizar operaciones intermedias antes de obtener una respuesta final. Además, permite
estructurar el comportamiento del agente de forma clara, separando las fases de razonamiento y ejecución, lo que facilita tanto su comprensión como su implementación.

En el contexto de este trabajo, este enfoque sirve como ejemplo de cómo \emph{LangGraph} permite integrar distintos componentes dentro de un mismo flujo de ejecución, combinando generación, evaluación y toma
de decisiones en un proceso iterativo.


%────────────────────── Ejemplos en LangGraph ──────────────────────────
\section{Ejemplos en LangGraph}

\subsection{Ejemplo Básico: Contador Iterativo}

Con el objetivo de ilustrar de forma práctica los conceptos introducidos a lo largo de este capítulo, se presenta a continuación un ejemplo sencillo de un flujo de ejecución implementado mediante \emph{LangGraph}.
Este ejemplo tiene un carácter puramente didáctico y busca mostrar de manera clara cómo se organizan los distintos elementos del sistema, sin introducir complejidad innecesaria.

El comportamiento modelado consiste en un proceso iterativo muy simple: partiendo de un valor inicial, el sistema incrementa dicho valor en cada paso hasta alcanzar una condición de parada. Aunque se trata de un
caso elemental, permite observar con claridad la interacción entre estado, nodos y transiciones dentro del grafo.

En primer lugar, se define la estructura del estado compartido. En este caso, el estado contiene únicamente un contador entero que será modificado a lo largo de la ejecución:

\begin{lstlisting}[language=Python, caption={Estructura del estado del grafo}]
class CounterState(TypedDict):
    counter: int
\end{lstlisting}

A continuación, se define el nodo principal del sistema. Este nodo recibe el estado actual, realiza una operación sobre él y devuelve una versión actualizada:

\begin{lstlisting}[language=Python, caption={Nodo principal del sistema}]
def increment_node(state: CounterState) -> dict:
    current = state["counter"]
    return {"counter": current + 1}
\end{lstlisting}

Este fragmento ilustra una de las ideas fundamentales de \emph{LangGraph}: Los nodos no modifican directamente el estado, sino que devuelven los cambios que deben aplicarse sobre él. De este modo, el sistema mantiene
un control explícito sobre la evolución de la información.

Para controlar el flujo de ejecución se introduce una función de decisión que determina si el proceso debe continuar o finalizar. Esta función se basa en el valor actual del contador:

\begin{lstlisting}[language=Python, caption={Condición de parada}]
def should_continue(state: CounterState) -> str:
    if state["counter"] < 5:
        return "continue"
    return "end"
\end{lstlisting}

Esta función permite modelar una transición condicional dentro del grafo, dando lugar a un comportamiento iterativo.

Una vez definidos el estado y las funciones principales, se construye el grafo mediante la conexión de los distintos elementos:

\begin{lstlisting}[language=Python, caption={Conexión de los elementos del grafo}]
builder = StateGraph(CounterState)

builder.add_node("increment", increment_node)

builder.set_entry_point("increment")

builder.add_conditional_edges(
    "increment",
    should_continue,
    {
        "continue": "increment",
        "end": END,
    },
)
\end{lstlisting}

En este caso, el grafo está compuesto por un único nodo que se ejecuta de forma repetida mientras se cumpla la condición establecida. La transición condicional permite modelar un bucle de manera explícita, sin necesidad
de recurrir a estructuras de control tradicionales como \texttt{while} o \texttt{for}.

Finalmente, el sistema se compila y se ejecuta a partir de un estado inicial:

\begin{lstlisting}[language=Python, caption={Ejecución de estado inicial}]
graph = builder.compile()

result = graph.invoke({"counter": 0})
\end{lstlisting}

Durante la ejecución, el valor del contador se incrementa progresivamente hasta alcanzar el límite definido, momento en el cual el flujo de ejecución finaliza.
A pesar de su simplicidad, este ejemplo permite apreciar cómo \emph{LangGraph} proporciona un marco estructurado para modelar procesos iterativos mediante grafos. En particular, se observa cómo el estado se va
actualizando de forma controlada, cómo los nodos encapsulan unidades de procesamiento y cómo las transiciones permiten definir el comportamiento dinámico del sistema.

Este tipo de enfoque resulta especialmente útil en escenarios más complejos, como los agentes basados en modelos de lenguaje, donde es necesario encadenar múltiples pasos de razonamiento y toma de decisiones dentro de un
mismo flujo de ejecución.


\subsection{Ejemplo con Flujo Condicional: Transformación de un Valor Numérico}

Tras el ejemplo anterior, basado en un contador sencillo, resulta conveniente introducir un caso algo más complejo que permita observar con mayor claridad cómo se combinan varios de los elementos descritos en este capítulo.
En particular, interesa mostrar una situación en la que intervengan distintos nodos, transiciones condicionales y un proceso iterativo controlado mediante el estado.

Con este objetivo, se plantea un ejemplo en el que el sistema parte de un número entero y aplica distintas transformaciones en función de su valor. Si el número es par, se divide entre dos; si es impar, se incrementa en una
unidad. El proceso se repite hasta que se alcanza el valor 1. Se trata de un caso deliberadamente simple, pero suficiente para ilustrar cómo \emph{LangGraph} permite modelar flujos de ejecución con varios caminos posibles dentro
de una misma estructura de grafo.

En primer lugar, se define el estado compartido del sistema. Además del valor actual, se incorpora un historial que permite registrar las transformaciones realizadas durante la ejecución:

\begin{lstlisting}[language=Python, caption={Estructura del estado del grafo}]
class NumberState(TypedDict):
    value: int
    history: List[str]
\end{lstlisting}

La inclusión de un historial resulta útil porque permite observar cómo el estado no solo almacena información puntual, sino también la evolución del proceso. De este modo, cada nodo puede consultar el valor actual y, al mismo
tiempo, dejar constancia de la operación efectuada.

A continuación, se define una función de decisión encargada de determinar a qué nodo debe dirigirse el flujo en cada iteración. Esta decisión depende del valor actual del estado:

\begin{lstlisting}[language=Python, caption={Función de decisión}]
def choose_operation(state: NumberState) -> str:
    if state["value"] == 1:
        return "end"
    elif state["value"] % 2 == 0:
        return "divide"
    return "increment"
\end{lstlisting}

Esta función ilustra cómo las transiciones condicionales permiten introducir comportamientos dinámicos dentro del grafo. El flujo no avanza siempre hacia el mismo nodo, sino que puede tomar distintos caminos según la información
almacenada en el estado.

Una vez definida la lógica de decisión, se implementan los nodos que realizan las transformaciones sobre el valor. El primero de ellos se encarga de dividir entre dos los números pares:

\begin{lstlisting}[language=Python, caption={Nodo divisor en valores pares}]
def divide_node(state: NumberState) -> dict:
    current = state["value"]
    new_value = current // 2

    return {
        "value": new_value,
        "history": state["history"] + [f"{current} es par, se divide entre 2 y pasa a {new_value}"],
    }
\end{lstlisting}

Este nodo recibe el estado, calcula el nuevo valor y devuelve únicamente las modificaciones que deben incorporarse al estado global. Junto con la actualización numérica, se añade también una entrada al historial que describe la operación
realizada.

De forma análoga, se define un segundo nodo para el caso en que el valor sea impar:

\begin{lstlisting}[language=Python, caption={Nodo de incremento en valores impares}]
def increment_node(state: NumberState) -> dict:
    current = state["value"]
    new_value = current + 1

    return {
        "value": new_value,
        "history": state["history"] + [f"{current} es impar, se incrementa en 1 y pasa a {new_value}"],
    }
\end{lstlisting}

La existencia de dos nodos distintos permite representar de forma explícita dos comportamientos diferentes dentro del mismo sistema. Esta separación mejora la claridad del diseño y facilita la expresión del grafo en caso de querer incorporar
nuevas operaciones más adelante.

Definidos el estado, la función de decisión y los nodos de procesamiento, puede construirse ya el grafo de ejecución:

\begin{lstlisting}[language=Python, caption={Conexión de los elementos del grafo}]
builder = StateGraph(NumberState)

builder.add_node("divide", divide_node)
builder.add_node("increment", increment_node)
\end{lstlisting}

A diferencia del ejemplo anterior, en este caso no existe un único nodo que se repite de manera uniforme, sino varios nodos posibles entre los que el sistema debe elegir en función del estado actual. Esta característica permite apreciar mejor el
papel de las aristas condicionales dentro de \emph{LangGraph}.

El flujo se inicia a partir de una decisión, que determina qué operación debe aplicarse en cada momento. Tras ejecutar cualquiera de los dos nodos, el sistema vuelve de nuevo al punto de decisión, formando así un ciclo:

\begin{lstlisting}[language=Python, caption={Flujo de ejecución}]
builder.set_conditional_entry_point(
    choose_operation,
    {
        "divide": "divide",
        "increment": "increment",
        "end": END,
    },
)

builder.add_conditional_edges(
    "divide",
    choose_operation,
    {
        "divide": "divide",
        "increment": "increment",
        "end": END,
    },
)

builder.add_conditional_edges(
    "increment",
    choose_operation,
    {
        "divide": "divide",
        "increment": "increment",
        "end": END,
    },
)
\end{lstlisting}

Este esquema refleja con claridad la lógica general del sistema. En primer lugar, se consulta el estado para decidir qué operación corresponde aplicar. A continuación, se ejecuta el nodo apropiado y, una vez actualizado el estado, se
vuelve a evaluar la situación. El proceso se repite hasta que la función de decisión indica que debe alcanzarse el estado final.

Por último, el grafo se compila y se ejecuta a partir de un estado inicial concreto:

\begin{lstlisting}[language=Python, caption={Ejecución de estado inicial}]
graph = builder.compile()

result = graph.invoke({
    "value": 7,
    "history": []
})
\end{lstlisting}

Durante la ejecución, el valor irá trasformándose sucesivamente en función de su paridad, mientras que el historial registrará cada uno de los pasos realizados. De este modo, no solo se obtiene el resultado final, sino también
una traza del proceso seguido por el sistema.

Este ejemplo permite apreciar de forma más completa el funcionamiento de \emph{LangGraph}. En él aparecen de manera conjunta varios de los conceptos fundamentales introducidos previamente: un estado compartido que evoluciona, nodos
que encapsulan operaciones específicas, transiciones condicionales que dirigen el flujo y un buvle que mantiene la ejecución activa hasta alcanzar una condición de finalización. Precisamente por ello, constituye una ilustración más
representativa del uso de grafos que un ejemplo puramente lineal, al tiempo que se mantiene lo bastante sencillo como para servir de apoyo didáctico dentro de este capítulo.











%────────────────────── Chapter 4 — Implementación, Experimentación y Refinamiento del Agente ────────────────────────────

\chapter{Implementación, Experimentación y Refinamiento del Agente}












%────────────────────── Diseño Estructural del Grafo de Razonamiento ──────────────────────────
\section{Diseño Estructural del Grafo de Razonamiento}

El sistema propuesto se basa en la construcción de un grafo iterativo utilizando la librería \emph{LangGraph}. Este grafo implementa un mecanismo inspirado en el algoritmo de \emph{Beam Search}, permitiendo generar, evaluar y
seleccionar múltiples estrofas candidatas a lo largo de varias iteraciones.

A diferencia de una aproximación directa, en la que se solicitaría al modelo una única estrofa final, el sistema introduce un proceso explícito de exploración y poda de alternativas. De este modo, el agente no depende únicamente
de la primera salida generada por el modelo, sino que mantiene varias hipótesis en paralelo y conserva aquellas que presentan un mejor cumplimiento de las restricciones formales.

En el contexto de este trabajo, cada trayectoria o \emph{beam} representa una posible estrofa de cuatro versos. La calidad de cada estrofa se determina a partir de criterios objetivos relacionados con el número de versos, la
métrica endecasílaba y el esquema de rima consonante ABBA.

\subsection{Arquitectura General del Grafo}

La figura~\ref{fig:procesoRazonamiento} muestra la estructura general del grafo implementado. El flujo se organiza como un ciclo compuesto por tres fases principales: generación, evaluación y selección.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/Imagen 4.1.png}
    \caption{Esquema del proceso de razonamiento basado en Beam Search con evaluación y poda de trayectorias. Fuente: elaboración propia}
    \label{fig:procesoRazonamiento}
\end{figure}

El sistema se organiza mediante tres nodos principales:

\begin{itemize}
\item \textbf{Nodo Generador (\emph{Expand})}: se encarga de generar nuevas estrofas candidatas. En la primera iteración, produce estrofas desde cero a partir de la instrucción inicial. En iteraciones posteriores, genera variantes o correcciones de estrofas ya existentes.
\item \textbf{Nodo Evaluador (\emph{Score})}: analiza cada estrofa candidata mediante un proceso de evaluación formal. En esta fase se comprueba el número de versos, la aproximación métrica al endecasílabo y el cumplimiento del esquema de rima ABBA. Además, se aplican las fases de reparación local cuando procede.
\item \textbf{Nodo Selector (\emph{Prune})}: ordena las estrofas candidatas según su puntuación y conserva únicamente las mejores $k$ alternativas. Esta operación permite limitar el crecimiento del espacio de búsqueda y continuar el proceso solo con las soluciones más prometedoras.
\end{itemize}

Este ciclo se repite hasta alcanzar el número máximo de iteraciones definido en la configuración del experimento.

\subsection{Implementación del Grafo en LangGraph}

La definición del grafo se realiza mediante la clase \texttt{StateGraph}. En ella se especifican los nodos que componen el flujo de ejecución y las transiciones entre ellos. El siguiente fragmento muestra una versión simplificada de la construcción del grafo:

\begin{lstlisting}[language=Python, caption={Definición del grafo de razonamiento con LangGraph}]
builder = StateGraph(BeamSearchState)

builder.add_node("expand", expand_node)
builder.add_node("score", score_node)
builder.add_node("prune", prune_node)

builder.set_entry_point("expand")

builder.add_edge("expand", "score")

builder.add_edge("score", "prune")

builder.add_conditional_edges(
    "prune",
    should_continue,
    {
        "continue": "expand",
        "end": END,
    },
)
\end{lstlisting}

Este fragmento refleja la estructura cíclica del sistema. Tras la generación de candidatos en el nodo \texttt{expand}, estos son evaluados en el nodo \texttt{score}. Posteriormente, el nodo \texttt{prune} selecciona las mejores estrofas y decide si el proceso debe continuar o finalizar.

La transición condicional situada después de \texttt{prune} permite implementar la lógica iterativa del agente. Si todavía no se ha alcanzado la condición de parada, el flujo vuelve al nodo \texttt{expand}. En caso contrario, la ejecución finaliza y se conserva la mejor estrofa encontrada.



\subsection{Representación del Estado}

El estado del grafo encapsula toda la información necesaria para mantener el proceso de búsqueda. Esta estructura permite almacenar varias estrofas candidatas en paralelo, junto con sus puntuaciones, trazas de ejecución y parámetros de control.

\begin{lstlisting}[language=Python, caption={Estructura del estado del grafo}]
class BeamSearchState(TypedDict):
    question: str
    beams: List[Dict[str, Any]]
    candidates: List[Dict[str, Any]]
    trace: List[Dict[str, Any]]
    step: int
    max_steps: int
    k: int
\end{lstlisting}

El campo \texttt{beams} almacena las mejores estrofas disponibles en cada iteración, mientras que \texttt{candidates} contiene las nuevas variantes generadas antes de aplicar la poda. Por otro lado, \texttt{trace} registra información relevante del proceso, lo que permite analizar posteriormente cómo ha evolucionado la búsqueda.

Los campos \texttt{step}, \texttt{max\_steps} y \texttt{k} controlan respectivamente la iteración actual, el número máximo de iteraciones y el número de alternativas que se conservan tras cada fase de selección.


\subsection{Flujo de Ejecución}

El flujo de ejecución del sistema puede resumirse en los siguientes pasos:

\begin{enumerate}
    \item El nodo \texttt{expand} genera nuevas estrofas candidatas a partir del estado actual.
    \item El nodo \texttt{score} evalúa formalmente cada candidata y aplica las reparaciones locales disponibles.
    \item El nodo \texttt{prune} ordena las candidatas según su puntuación y conserva las mejores $k$.
    \item El sistema comprueba la condición de parada.
    \item Si no se ha alcanzado el límite de iteraciones, el proceso vuelve al nodo \texttt{expand}.
\end{enumerate}

Este diseño permite que el agente explore diferentes alternativas antes de seleccionar una solución final. Además, al conservar varias estrofas candidatas en paralelo, se reduce la dependencia de una única generación del modelo y se facilita la recuperación ante salidas parciales o formalmente incorrectas.

\subsection{Ventajas del Enfoque}

La arquitectura basada en grafo presenta varias ventajas. En primer lugar, permite separar de forma clara las responsabilidades de generación, evaluación y selección. Esta separación facilita la modificación independiente de cada fase del sistema, por ejemplo, cambiando la estrategia de prompting sin alterar la función de evaluación,
o ajustando el criterio de poda sin modificar el nodo generador.

En segundo lugar, el uso de \emph{Beam Search} permite mantener varias hipótesis activas durante el proceso. Esto resulta especialmente útil en tareas de generación poética con restricciones formales, donde una estrofa puede ser prometedora en términos de rima pero presentar errores métricos, o viceversa.

Por último, la estructura del grafo facilita el registro de trazas intermedias. Estas trazas permiten estudiar qué candidatos se generaron, qué puntuaciones recibieron, qué reparaciones se aplicaron y por qué determinadas estrofas fueron descartadas. Esta información resulta fundamental para la fase experimental y para el análisis
de errores desarrollado en apartados posteriores.



%────────────────────── Implementación de la Lógica de los Nodos y Prompting ──────────────────────────
\section{Implementación de la Lógica de los Nodos y Prompting}

Una vez definida la estructura general del grafo de razonamiento, el siguiente paso consiste en concretar la lógica interna de cada uno de sus nodos. Aunque la arquitectura global puede resumirse como un ciclo de generación, evaluación y selección, el
comportamiento real del sistema depende de cómo se implementa cada fase y de cómo se comunica el agente con el modelo de lenguaje local.

En este trabajo, la generación de texto se realiza mediante un modelo ejecutado localmente con \emph{Ollama}. Esta decisión permite mantener todo el proceso de inferencia en el equipo de desarrollo, sin depender de servicios externos.
El modelo se utiliza principalmente como generador de estrofas y como mecanismo de reparación lingüística, mientras que la evaluación formal de las salidas se realiza mediante funciones objetivas implementadas en \emph{Python}.

La unidad de trabajo del sistema no es un verso aislado ni un soneto completo, sino una estrofa de cuatro versos. Cada estrofa candidata debe cumplir tres restricciones principales: estar formada por exactamente cuatro versos, aproximarse al patrón
endecasílabo y respetar una rima consonante ABBA. Por tanto, los prompts diseñados para el modelo no solo deben pedir una salida poética, sino también imponer un formato controlado y restricciones formales explícitas.

\subsection{Integración con \emph{Ollama}}

La integración con Ollama se realiza mediante llamadas locales al modelo de lenguaje configurado para la generación. En la versión actual del sistema, el modelo utilizado es \texttt{mistral:7b-instruct}, ejecutado de forma local.
Esta configuración permite realizar múltiples llamadas durante una misma ejecución del grafo, tanto para generar estrofas iniciales como para producir variantes o reparar errores detectados posteriormente.

El uso de Ollama introduce una separación clara entre dos responsabilidades. Por un lado, el modelo de lenguaje se encarga de producir contenido textual: estrofas completas, variantes corregidas o versos alternativos.
Por otro lado, el sistema implementado en \emph{Python} se encarga de validar objetivamente si esas salidas cumplen las restricciones exigidas. Esta separación resulta importante, ya que evita confiar exclusivamente en la capacidad del modelo para juzgar sus propias respuestas.

En otras palabras, el modelo no actúa como evaluador principal del sistema. Su papel se centra en la generación y reformulación de texto. La comprobación de la calidad formal se delega en funciones específicas de evaluación, capaces de medir el número de versos,
estimar la métrica y comprobar el esquema de rima. De este modo, el sistema combina la flexibilidad generativa del LLM con un mecanismo de control externo más determinista.

Además, las llamadas a Ollama se configuran mediante parámetros como la temperatura y el número máximo de tokens generados. La temperatura controla el grado de variabilidad de las respuestas. Una temperatura baja favorece salidas más estables y conservadoras,
mientras que una temperatura mayor incrementa la diversidad de las variantes generadas. En el contexto de este trabajo, este parámetro resulta especialmente relevante, ya que el sistema necesita explorar alternativas, pero sin perder por completo el control sobre la forma de la estrofa.

El siguiente fragmento muestra una versión simplificada de la función encargada de realizar la llamada al modelo:

\begin{lstlisting}[language=Python, caption={Función de comunicación con Ollama}]
def chat_ollama(
    model: str,
    messages: List[Dict[str, str]],
    temperature: float = TEMPERATURE,
    num_predict: int = NUM_PREDICT,
) -> str:
    """Llama a Ollama por HTTP y fuerza una respuesta JSON."""
    payload = {
        "model": model,
        "messages": messages,
        "stream": False,
        "format": "json",
        "options": {
            "temperature": temperature,
            "num_predict": num_predict,
        },
    }

    response = requests.post(f"{BASE_URL}/api/chat", json=payload, timeout=240)
    response.raise_for_status()

    data = response.json()
    return data["message"]["content"].strip()

\end{lstlisting}

Esta función recibe una lista de mensajes en formato conversacional y devuelve el contenido textual generado por el modelo. A partir de esta función básica se construyen las distintas operaciones de generación y reparación utilizadas por el agente.

%La Figura~\ref{fig:LogicaNodosyPrompting} resume el proceso de interacción entre los prompts generados por el sistema y el modelo local ejecutado mediante Ollama.

%\begin{figure}[h]
%    \centering
%    \includegraphics[width=0.5\textwidth]{images/LogicaNodosyPrompting.png}
%    \caption{Flujo de interacción entre el sistema y el modelo local ejecutado mediante Ollama. Fuente: elaboración propia.}
%    \label{fig:LogicaNodosyPrompting}
%\end{figure}


% ==================================================================================================================================


\subsection{Lógica del Nodo Generador}

El nodo generador, denominado \texttt{expand}, es el encargado de producir nuevas estrofas candidatas. Su comportamiento varía en función de la iteración en la que se encuentre el grafo.

En la primera iteración, el sistema todavía no dispone de estrofas previas. Por ello, el nodo generador solicita al modelo que produzca una o varias estrofas desde cero.
El prompt inicial describe de forma explícita la tarea: generar una estrofa de cuatro versos en español, con versos endecasílabos y rima consonante ABBA. La salida generada se considera una propuesta candidata, no una solución final garantizada.

En iteraciones posteriores, el comportamiento del nodo cambia. En lugar de solicitar una estrofa completamente nueva sin contexto, el sistema proporciona al modelo una estrofa previa y le pide que genere una variante corregida o mejorada.
De esta forma, el proceso se vuelve iterativo: cada nueva generación parte de una hipótesis anterior y trata de mejorarla.

Este diseño permite que el agente no dependa únicamente de la primera respuesta del modelo. Si una estrofa initial presenta errores de métrica o de rima, puede seguir siendo útil como punto de partida para generar una versión posterior más adecuada.
Por tanto, el nodo generador cumple una doble función: exploración inicial del espacio de soluciones y refinamiento progresivo de las mejores trayectorias encontradas.

La lógica del nodo puede resumirse de la siguiente forma:

\begin{itemize}
    \item Si no existen estrofas previas, se generan candidatos iniciales desde cero.
    \item Si ya existen \emph{beams}, se generan nuevas variantes a partir de las estrofas mejor valoradas.
    \item Cada estrofa generada se almacena como candidata para su posterior evaluación.
    \item Las candidatas no se aceptan directamente como solución final, sino que pasan al nodo evaluador.
\end{itemize}

Esta distinción entre generación inicial y generación condicionada es fundamental para el funcionamiento del sistema. En una aproximación directa, el modelo solo tendría una oportunidad para satisfacer todas las restricciones.
En cambio, con esta arquitectura, cada iteración permite corregir errores, conservar avances parciales y explorar diferentes alternativas.


\subsection{Estrategias de Prompting}

El diseño de los prompts tiene un papel central en el comportamiento del sistema. Dado que los modelos de lenguaje no siempre respetan instrucciones formales de manera estricta, los prompts deben formularse de forma clara, restrictiva y orientada a producir salidas fácilmente procesables por el programa.

En el sistema se emplean diferentes tipos de prompts según la fase del proceso. No se utiliza un único prompt general para toda la ejecución, sino varios prompts especializados en función de la tarea concreta que debe realizar el modelo.

En primer lugar, se encuentra el prompt de generación inicial. Este prompt solicita una estrofa completa de cuatro versos y especifica las restricciones principales de la tarea. Su objetivo es obtener una primera propuesta que, idealmente, cumpla desde el inicio el número de versos, la métrica y la rima.
Sin embargo, debido a las limitaciones del modelo, esta salida se trata únicamente como una candidata que debe ser evaluada después.

En segundo lugar, se utilizan prompts de corrección o refinamiento. Estos prompts se aplican cuando el sistema ya dispone de una estrofa previa. En este caso, el modelo recibe información sobre la estrofa anterior y se le pide que genere una versión mejorada.
La finalidad no es producir una estrofa completamente distinta, sino corregir los aspectos que impiden cumplir las restricciones formales.

En tercer lugar, existen prompts de reparación local. Estos prompts se centran en una parte concreta de la estrofa, como un verso que no alcanza las once sílabas métricas o una pareja de versos que no cumple la rima esperada.
Esta estrategia reduce la libertad del modelo y evita que modifique partes de la estrofa que ya eran válidas.

El uso de prompts especializados permite adaptar el comportamiento del modelo a cada fase del proceso. Por ejemplo, cuando el problema detectado es métrico, el prompt puede pedir variantes de un verso concreto intentando acercarlo a once sílabas.
Cuando el problema está en la rima, el prompt puede pedir que se conserve un verso ancla y se modifique únicamente el verso reparable.

Esta estrategia es especialmente útil en tareas poéticas con restricciones formales. Si se permite al modelo reescribir toda la estrofa en cada intento, es frecuente que corrija un error pero introduzca otros nuevos.
Por ejemplo, puede mejorar la rima exterior pero estropear la métrica de un verso que ya era correcto. Por ello, el sistema tiende a aplicar correcciones locales siempre que es posible.

La Figura~\ref{fig:EstrategiasPromptingAgente} resume las principales estrategias de \emph{prompting} utilizadas por el sistema. Como puede observarse, no se emplea una única instrucción general para todo el proceso, sino diferentes prompts especializados según la fase de ejecución:
generación inicial, corrección iterativa y reparación local. Esta separación permite adaptar la interacción con el modelo a cada tipo de tarea y reducir la probabilidad de modificar partes de la estrofa que ya cumplen las restricciones formales.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\textwidth]{images/EstrategiasPromptingAgente.png}
    \caption{Estrategias de prompting utilizadas por el agente durante el proceso de generación y refinamiento de estrofas. Fuente: elaboración propia}
    \label{fig:EstrategiasPromptingAgente}
\end{figure}



\subsection{Formato de Salida y Control de Respuestas}

Otro aspecto importante del prompting es el formato de salida. Para que el sistema pueda procesar automáticamente las respuestas del modelo, es necesario que estas sean lo más limpias y estructuradas posible.
En este trabajo se evita que el modelo devuelva explicaciones extensas, comentarios adicionales o análisis literarios mezclados con la estrofa.

Por esta razón, los prompts insisten en que la salida debe contener únicamente la estrofa o el verso solicitado, según corresponda. Esta restricción facilita el procesamiento posterior, ya que el sistema puede limpiar la respuesta, separar los versos y enviarlos a las funciones de evaluación.

A pesar de estas instrucciones, los modelos de lenguaje pueden producir salidas con elementos no deseados, como numeraciones, comillas, títulos o frases introductorias.
Por ello, el sistema incluye pasos de limpieza y normalización antes de evaluar cada candidata. Estos pasos permiten reducir el impacto de pequeñas desviaciones de formato y mejorar la robustez general del proceso.

El control del formato de salida cumple, por tanto, una doble función. Por un lado, facilita la integración técnica entre el modelo y el código \emph{Python}. Por otro lado, reduce la probabilidad de que errores superficiales de formato afecten negativamente a la evaluación formal de una estrofa.


\subsection{Lógica del Nodo Evaluador}

El nodo evaluador, denominado \texttt{score}, recibe las estrofas candidatas generadas por el nodo anterior y les asigna una puntuación. Aunque el desarrollo detallado de la función de puntuación se presenta en el apartado siguiente, resulta conveniente introducir aquí su papel dentro del flujo del grafo.

La función del nodo evaluador no se limita a calcular una puntuación final. En la versión actual del sistema, este nodo también aplica un conjunto de reparaciones locales antes de obtener la evaluación definitiva.
Esto significa que una estrofa candidata puede ser modificada durante la fase de evaluación si se detectan errores corregibles.

El proceso general seguido por este nodo puede resumirse en varias fases. En primer lugar, se analiza la estructura básica de la estrofa, comprobando si contiene el número esperado de versos.
A continuación, se evalúa la métrica de cada verso y, si procede, se intentan reparaciones locales para acercar los versos al patrón endecasílabo. Después, se diagnostica la rima exterior A, correspondiente a los versos primero y cuarto, y la rima interior B,
correspondiente a los versos segundo y tercero. En caso de fallo, se pueden activar reparaciones específicas para cada pareja de rima.

Esta lógica convierte el nodo evaluador en una fase de validación y refinamiento. No se limita a descartar salidas incorrectas, sino que intenta aprovechar aquellas candidatas que contienen partes útiles.
Por ejemplo, una estrofa puede presentar una buena rima exterior pero fallar en la rima interior, o puede tener una estructura correcta pero algunos versos con errores métricos. En esos casos, las reparaciones locales permiten intentar mejorar la candidata sin reiniciar completamente el proceso.

Una característica importante de esta fase es que las reparaciones se aplican de forma controlada. Cuando se repara la rima exterior, el sistema conserva el primer verso como ancla y modifica únicamente el cuarto.
De forma análoga, cuando se repara la rima interior, se conserva el segundo verso y se modifica el tercero. Esta estrategia reduce el riesgo de perder partes válidas de la estrofa y permite que las mejoras sean acumulativas.


\subsection{Lógica del Nodo Selector}

El nodo selector, denominado \texttt{prune}, se encarga de aplicar la poda propia de Beam Search. Una vez que las candidatas han sido evaluadas, el sistema ordena las estrofas según su puntuación y conserva únicamente las mejores $k$ alternativas.

Esta fase es necesaria porque el número de candidatas puede crecer rápidamente a medida que avanzan las iteraciones. Si cada beam genera varias variantes, el espacio de búsqueda aumenta de forma exponencial.
La poda permite limitar este crecimiento y mantener el proceso dentro de un coste computacional asumible.

El valor de $k$ representa el ancho del haz o \emph{beam width}. Un valor pequeño reduce el tiempo de ejecución, pero también limita la exploración del espacio de soluciones.
Un valor mayor permite conservar más alternativas, aunque incrementa el número de llamadas al modelo y el coste total del proceso. Por tanto, este parámetro introduce un compromiso directo entre calidad potencial y coste computacional.

Además de seleccionar las mejores estrofas, el nodo selector actualiza el estado del grafo para preparar la siguiente iteración. Las candidatas seleccionadas pasan a convertirse en los nuevos \emph{beams},
mientras que el contador de iteraciones avanza. A partir de ese momento, el nodo generador podrá producir nuevas variantes tomando como referencia las mejores estrofas conservadas.


%\subsection{Gestión del Estado entre Nodos}

%La gestión del estado es uno de los aspectos más importantes de la implementación. En LangGraph, los nodos no trabajan de forma aislada, sino que reciben y devuelven un objeto de estado compartido. Este objeto contiene toda la información necesaria para que el proceso pueda avanzar de una fase a otra sin perder el contexto.

%En este sistema, el estado almacena principalmente las estrofas activas, las candidatas generadas, la traza de ejecución y los parámetros de control del algoritmo. Gracias a esta estructura, el grafo puede mantener varias trayectorias en paralelo sin que se mezclen entre sí.

%Cada beam representa una posible solución parcial o candidata final. Para cada uno de ellos se puede almacenar la estrofa asociada, su puntuación, su historial de puntuaciones y la información de diagnóstico generada durante la evaluación. Esta representación permite comparar diferentes alternativas de forma ordenada y conservar información útil para el análisis experimental posterior.

%El uso de una estructura de estado explícita presenta varias ventajas. En primer lugar, permite que cada nodo tenga una responsabilidad clara. El nodo generador lee los beams actuales y escribe nuevas candidatas. El nodo evaluador lee las candidatas y les añade puntuaciones y diagnósticos. El nodo selector ordena esas candidatas y actualiza la lista de beams. En segundo lugar, facilita la depuración del sistema, ya que la traza permite reconstruir qué ocurrió en cada iteración.

%Además, la gestión del estado resulta esencial para evitar colisiones entre trayectorias. Si varias estrofas se exploran en paralelo, cada una debe conservar su propia información. De lo contrario, una reparación aplicada a una candidata podría afectar indebidamente a otra. Por ello, cada trayectoria se trata como una entidad independiente dentro de la lista de beams.


\subsection{Trazabilidad del Proceso} % Archivos de salida generados en la carpeta /outputs

Una de las ventajas de implementar el agente mediante un grafo explícito es la posibilidad de registrar información intermedia durante la ejecución. El sistema no solo produce una estrofa final, sino que también genera trazas que permiten analizar cómo se ha llegado a ella.

Estas trazas incluyen información sobre las fases de expansión, evaluación y poda. Por ejemplo, permiten saber qué candidatas se generaron en una iteración, qué puntuación obtuvo cada una, qué reparaciones se aplicaron y cuáles fueron finalmente seleccionadas para continuar.
Esta información resulta especialmente útil para el análisis de errores y para la comparación entre distintas configuraciones experimentales.

Desde el punto de vista de la memoria del proyecto, la trazabilidad cumple un papel fundamental. No basta con mostrar la estrofa final generada por el sistema; también interesa estudiar el comportamiento del agente durante el proceso.
Las trazas permiten justificar por qué una determinada configuración funciona mejor que otra, detectar patrones de fallo y observar el efecto real de las reparaciones introducidas.

Por ejemplo, si una estrofa final no cumple correctamente la rima ABBA, la traza puede ayudar a determinar si el fallo procede de la generación inicial, de una reparación de rima fallida o de una poda que descartó una alternativa mejor. Del mismo modo, si el tiempo de ejecución aumenta considerablemente,
las trazas pueden mostrar si el incremento se debe a un mayor número de variantes, a reparaciones adicionales o a un ancho de haz demasiado elevado.

\subsection{Relación entre Prompting, Estado y Beam Search}

La lógica de prompting, la gestión del estado y el algoritmo Beam Search no son componentes independientes, sino partes conectadas del mismo proceso. Los prompts determinan qué tipo de variantes genera el modelo. El estado permite conservar varias de esas variantes en paralelo.
Finalmente, \emph{Beam Search} decide cuáles merecen continuar en la siguiente iteración.

Esta relación puede interpretarse como un ciclo de refinamiento progresivo. El modelo propone soluciones, el sistema las evalúa de forma objetiva y el grafo conserva las alternativas más prometedoras. En cada iteración, las mejores estrofas sirven como punto de partida para nuevas generaciones.
De este modo, el sistema combina exploración y explotación: explora distintas posibilidades generadas por el modelo, pero concentra progresivamente la búsqueda en aquellas que presentan mejores resultados formales.

Este enfoque resulta adecuado para la generación de estrofas con restricciones, ya que el cumplimiento de la tarea no depende únicamente de la creatividad del modelo. La arquitectura fuerza un proceso de revisión, comparación y selección que permite corregir errores frecuentes en las salidas de los LLMs,
como el incumplimiento del número de versos, desviaciones métricas o rimas incorrectas.

En resumen, la implementación de los nodos no se limita a conectar llamadas a un modelo de lenguaje. El sistema construye un flujo de trabajo controlado en el que cada nodo cumple una función concreta: generar alternativas, evaluarlas, repararlas cuando es posible y seleccionar las más adecuadas para continuar.
Esta organización modular facilita la experimentación posterior y permite introducir mejoras de forma progresiva, como se analizará en los siguientes apartados.
















%────────────────────── Desarrollo de la Función de Puntuación (Scoring) ──────────────────────────

\section{Desarrollo de la Función de Puntuación \emph{(Scoring)}}

La función de puntuación constituye uno de los elementos centrales del sistema, ya que determina qué estrofas candidatas deben conservarse y cuáles deben descartarse durante el proceso de búsqueda.
En una arquitectura basada en \emph{Beam Search}, no basta con generar múltiples alternativas: es necesario disponer de un criterio que permita compararlas entre sí de forma consistente.

En este trabajo, la puntuación no se basa en una valoración subjetiva del modelo de lenguaje, sino en una evaluación formal objetiva. Aunque inicialmente se contempló la posibilidad de utilizar un enfoque de tipo \emph{LLM-as-a-judge},
en el que un modelo actuase como evaluador de las respuestas generadas, finalmente se optó por una estrategia más controlada y reproducible. Esta decisión resulta especialmente adecuada para el problema planteado, ya que las restricciones principales de la tarea pueden medirse de forma explícita:
número de versos, métrica aproximada y rima consonante ABBA.

De esta forma, el modelo de lenguaje se utiliza principalmente como generador y reparador de texto, mientras que la valoración de cada estrofa se realiza mediante funciones programadas en Python. Esta separación permite reducir la dependencia de juicios ambiguos del propio modelo y facilita
la comparación entre diferentes configuraciones experimentales.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Criterios de Evaluación Formal}

La unidad evaluada por el sistema es siempre una estrofa completa de cuatro versos. Cada candidata se analiza teniendo en cuenta tres criterios principales:

\begin{itemize}
    \item \textbf{Número de versos}: la estrofa debe contener exactamente cuatro versos.
    \item \textbf{Métrica}: cada verso debe aproximarse al patrón endecasílabo, es decir, tener once sílabas métricas.
    \item \textbf{Rima consonante}: la estrofa debe seguir el esquema ABBA, donde el primer verso rima con el cuarto y el segundo verso rima con el tercero.
\end{itemize}

Estos criterios permiten transformar una tarea aparentemente creativa en un problema evaluable de forma automática. La calidad de una estrofa no se mide únicamente por su fluidez literaria, sino por su grado de cumplimiento de unas restricciones formales previamente definidas.

El primer criterio comprueba la estructura básica de la salida. Esta comprobación es necesaria porque los modelos de lenguaje pueden generar respuestas con un número incorrecto de versos, añadir explicaciones o dividir el texto de forma inesperada.
Una estrofa que no contiene exactamente cuatro versos no puede considerarse válida dentro del marco definido para este proyecto.

El segundo criterio evalúa la métrica de cada verso. Para ello, el sistema emplea una estimación automática del número de sílabas métricas. Esta estimación tiene en cuenta aspectos como la separación silábica, la palabra final del verso y una aproximación básica a la sinalefa.
Aunque la métrica poética en español puede presentar fenómenos complejos, esta aproximación resulta suficiente para establecer un criterio operativo de comparación entre candidatas.

El tercer criterio analiza la rima consonante. En el esquema ABBA, los versos primero y cuarto deben compartir rima, mientras que los versos segundo y tercero deben rimar entre sí. Para comprobarlo, el sistema extrae la terminación rimática de la última palabra de cada verso,
tomando como referencia la vocal tónica estimada. Posteriormente, compara las terminaciones correspondientes para determinar si se cumple el patrón esperado.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Evaluación Objetiva frente a \emph{LLM-as-a-Judge}}

El uso de un evaluador basado en \emph{LLM} podría resultar atractivo en tareas abiertas, especialmente cuando se desea valorar aspectos difíciles de formalizar, como la coherencia semántica, la originalidad o la calidad literaria. Sin embargo, en este proyecto la mayor parte de los
requisitos principales son verificables mediante reglas concretas.

Por este motivo, se decidió no utilizar el modelo como juez principal del proceso. Delegar la evaluación formal en el propio \emph{LLM} podría introducir inconsistencias, ya que el modelo puede interpretar de forma variable las instrucciones,
equivocarse al contar sílabas o aceptar rimas incorrectas como válidas. En cambio, una función de evaluación programada permite aplicar siempre los mismos criterios sobre todas las candidatas.

Esta decisión aporta varias ventajas. En primer lugar, mejora la reproducibilidad de los experimentos, ya que dos estrofas iguales reciben la misma puntuación independientemente del momento de la ejecución. En segundo lugar, facilita el análisis de errores,
puesto que el sistema puede indicar qué restricción concreta se incumple. Por último, permite comparar de manera más clara distintas configuraciones del agente, como cambios en los prompts, en el ancho del haz (\emph{beam width}) o en los parámetros de generación.

No obstante, esta elección también implica ciertas limitaciones. La evaluación objetiva se centra en aspectos formales y no captura completamente la calidad estética o semántica de la estrofa. Una estrofa puede cumplir la métrica y la rima, pero resultar poco natural o tener un significado débil.
Esta limitación se asume como parte del alcance del trabajo, ya que el objetivo principal del sistema es estudiar el razonamiento iterativo y la satisfacción de restricciones formales mediante un agente basado en \emph{LangGraph} y \emph{Beam Search}.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Cálculo de la Puntuación}

La puntuación de cada estrofa se obtiene mediante una función específica de evaluación. Esta función analiza la candidata y devuelve un resultado estructurado con la puntuación global, una puntuación normalizada sobre veinte, los errores detectados y un mensaje de retroalimentación formal.

El proceso de evaluación puede resumirse en las siguientes fases:

\begin{enumerate}
    \item Limpieza y normalización de la estrofa generada.
    \item Separación del texto en versos.
    \item Comprobación del número de versos.
    \item Estimación de las sílabas métricas de cada verso.
    \item Extracción de la rima de la última palabra de cada verso.
    \item Verificación del esquema ABBA.
    \item Cálculo de una puntuación global.
    \item Generación de feedback formal para orientar posibles correcciones.
\end{enumerate}

El Listado~\ref{lst:evaluateStanzaAbba} muestra un fragmento simplificado de la función encargada de evaluar una estrofa candidata. Esta función centraliza la comprobación de las restricciones formales y devuelve una estructura con la puntuación y los errores detectados.

\begin{lstlisting}[language=Python, caption={Fragmento simplificado de la evaluación formal de una estrofa ABBA}, label={lst:evaluateStanzaAbba}]

def evaluate_stanza_abba(stanza: str | list[str]) -> dict[str, object]:
 
    verses = normalize_verses_input(stanza)
    verse_count_evaluation = evaluate_stanza_verse_count(verses)
    syllable_evaluation = evaluate_stanza_syllable_count(verses)
    rhyme_evaluation = evaluate_stanza_rhyme_scheme(verses)

    raw_score = (
        float(verse_count_evaluation["score"]) * VERSE_COUNT_WEIGHT
        + float(syllable_evaluation["score"]) * SYLLABLE_WEIGHT
        + float(rhyme_evaluation["score"]) * RHYME_WEIGHT
    )

    score = round(_clamp_score(raw_score), 4)

\end{lstlisting}

El resultado de esta evaluación permite comparar estrofas que pueden fallar de formas distintas. Por ejemplo, una candidata puede tener cuatro versos y buena rima, pero presentar errores métricos. Otra puede tener versos endecasílabos, pero no cumplir el esquema ABBA.
La puntuación global permite ordenar ambas alternativas y decidir cuál resulta más prometedora para continuar el proceso.

En la implementación actual, la evaluación formal no se utiliza únicamente al final de la ejecución. Por el contrario, se aplica en cada iteración del grafo. Esto permite que el sistema tome decisiones intermedias informadas y no espere hasta el final para detectar errores graves.
Así, el \emph{Beam Search} puede dirigir la búsqueda hacia las estrofas que muestran un mayor cumplimiento de las restricciones.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Feedback Formal y Reparación de Candidatas}

Además de asignar una puntuación, la función de evaluación produce información diagnóstica sobre los errores encontrados. Esta información resulta fundamental para las fases de reparación, ya que permite saber qué aspecto concreto de la estrofa debe modificarse.

Por ejemplo, si un verso no tiene once sílabas métricas, el sistema puede activar una reparación local centrada únicamente en ese verso. Si la rima exterior A falla, se conserva el primer verso como ancla y se intenta reescribir el cuarto.
De forma similar, si falla la rima interior B, se conserva el segundo verso y se intenta modificar el tercero.

Este enfoque convierte la función de puntuación en algo más que un mecanismo de ordenación. La evaluación actúa también como una fuente de información para guiar el refinamiento de las candidatas. En lugar de descartar directamente una estrofa parcialmente incorrecta,
el sistema puede intentar repararla si contiene elementos aprovechables.

La retroalimentación formal generada por el evaluador permite, por tanto, cerrar el ciclo entre evaluación y generación. El modelo propone una estrofa, el sistema detecta sus errores y, si procede, se construyen nuevas instrucciones para corregirlos.
Este comportamiento resulta especialmente relevante en tareas con restricciones formales, donde una solución puede estar cerca de ser válida aunque todavía incumpla uno de los requisitos.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Puntuación y Beam Search}

Dentro del algoritmo de \emph{Beam Search}, la puntuación cumple la función de guiar la exploración. Tras cada fase de generación y evaluación, el sistema dispone de un conjunto de candidatas con diferentes niveles de calidad formal. El nodo selector ordena estas candidatas según su puntuación
y conserva únicamente las mejores $k$.

Este mecanismo permite limitar el crecimiento del espacio de búsqueda. Si todas las variantes generadas se mantuvieran activas, el número de trayectorias aumentaría rápidamente en cada iteración. La poda basada en puntuación permite concentrar los recursos computacionales en
las estrofas más prometedoras.

El valor de $k$ determina cuántas alternativas se conservan en cada iteración. Si $k$ es demasiado pequeño, el sistema puede descartar pronto una estrofa que, aunque inicialmente no sea la mejor, podría haber mejorado en iteraciones posteriores. Si $k$ es demasiado grande,
el sistema explora más alternativas, pero aumenta el número de llamadas al modelo y, por tanto, el tiempo total de ejecución.

La función de puntuación es, por tanto, el criterio que conecta la evaluación formal con la estrategia de búsqueda. Una puntuación adecuada permite que el sistema avance hacia soluciones cada vez más cercanas al objetivo, mientras que una puntuación mal diseñada podría
favorecer candidatas que no conduzcan a una estrofa válida.

%La Figura~\ref{fig:ScoringBeamSearch} muestra el papel de la función de puntuación dentro del proceso de \emph{Beam Search}. Cada estrofa candidata es evaluada mediante criterios formales y, a partir de esa puntuación, el sistema decide qué trayectorias continúan activas en la siguiente iteración.

%\begin{figure}[h]
%    \centering
%    \includegraphics[width=0.85\textwidth]{images/ScoringBeamSearch.png}
%    \caption{Papel de la función de puntuación en la selección de las mejores estrofas candidatas. Fuente: elaboración propia}
%    \label{fig:ScoringBeamSearch}
%\end{figure}

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Ordenación y Selección de las Mejores Candidatas}

Una vez evaluadas todas las candidatas, el sistema procede a ordenarlas de mayor a menor puntuación. Las mejores alternativas se conservan como nuevos \emph{beams}, mientras que el resto se descarta. Esta operación constituye la fase de poda del algoritmo.

Conceptualmente, el proceso puede representarse de la siguiente forma:

\begin{lstlisting}[language=Python, caption={Esquema simplificado de selección de las mejores candidatas}]
scored_candidates = evaluate_candidates(candidates)

sorted_candidates = sorted(
        selection_pool,
        key=lambda beam: beam["score"],
        reverse=True,
    )

top_k = sorted_candidates[: state["k"]]
\end{lstlisting}

El fragmento anterior muestra una versión simplificada del mecanismo de selección. En la implementación completa, cada candidata puede contener más información además de la puntuación, como la estrofa generada, el historial de puntuaciones, los diagnósticos formales y los reportes de reparación.
Sin embargo, la idea central es la misma: ordenar las alternativas evaluadas y conservar solo las más prometedoras.

Esta selección se realiza en cada iteración del grafo. Por tanto, una estrofa no necesita ser perfecta desde el primer momento para sobrevivir, pero sí debe obtener una puntuación suficientemente alta como para mantenerse dentro de las mejores alternativas.
Esto permite que el sistema combine exploración y refinamiento progresivo.

\subsection{Protección de \emph{Beams} Prometedores}

Durante el desarrollo del sistema se observó que algunas estrofas parcialmente buenas podían perderse tras una iteración de generación o reparación. Esto ocurría cuando una estrofa con buena puntuación generaba variantes peores, provocando que la trayectoria original desapareciera del conjunto de
beams activos.

Para evitar este problema, se introdujo una estrategia de preservación de \emph{beams} prometedores. La idea consiste en conservar ciertas estrofas de alta calidad aunque sus descendientes inmediatos no mejoren el resultado. Esta técnica se puede interpretar como una forma de elitismo
dentro del proceso de búsqueda.

El elitismo resulta útil porque las llamadas al modelo no garantizan una mejora monotónica. Una variante generada a partir de una buena estrofa puede corregir un aspecto, pero empeorar otro. Por ejemplo, puede mejorar una rima pero alterar la métrica, o mantener la métrica pero cambiar
una palabra final que era importante para el esquema ABBA. En estos casos, conservar el beam original evita perder una solución que todavía puede ser competitiva.

Esta protección también contribuye a estabilizar el comportamiento del sistema. En tareas de generación con LLMs, las salidas pueden variar incluso bajo instrucciones similares. Mantener los mejores \emph{beams} encontrados permite que el proceso no dependa exclusivamente de la última
generación producida por el modelo.

\subsection{Interpretación de la Puntuación}

La puntuación obtenida por una estrofa debe interpretarse como una medida de cumplimiento formal, no como una valoración literaria completa. Una puntuación alta indica que la candidata se aproxima bien a los requisitos definidos: cuatro versos, métrica endecasílaba y rima ABBA.
Sin embargo, no garantiza necesariamente que la estrofa sea estética, profunda o semánticamente perfecta.

Esta distinción es importante para delimitar correctamente el alcance del sistema. El objetivo del agente no es sustituir el juicio literario humano, sino estudiar cómo un modelo de lenguaje puede ser guiado mediante un proceso iterativo de generación, evaluación y selección.
En este contexto, la puntuación formal proporciona una señal clara y automatizable para dirigir el \emph{Beam Search}.

En consecuencia, los resultados experimentales deben analizarse teniendo en cuenta esta perspectiva. Una mejora en la puntuación media de las estrofas indica que el sistema cumple mejor las restricciones formales, pero no implica necesariamente una mejora equivalente en calidad poética.
Esta limitación abre la puerta a posibles extensiones futuras, como incorporar métricas semánticas o una valoración humana complementaria.

\subsection{Importancia del Scoring en el Sistema}

El desarrollo de la función de puntuación permite transformar la generación poética en un proceso de búsqueda guiada. Sin este componente, el sistema se limitaría a solicitar respuestas al modelo y aceptar la salida obtenida. En cambio, al disponer de una señal de evaluación,
el agente puede comparar alternativas, conservar las mejores y orientar las iteraciones posteriores.

El scoring actúa como punto de conexión entre las restricciones formales del problema y la arquitectura de razonamiento implementada en LangGraph. Los prompts generan diversidad, el estado mantiene varias trayectorias activas y la puntuación decide cuáles de ellas merecen continuar.
Por ello, la calidad de la función de puntuación influye directamente en el comportamiento global del agente.

En resumen, la función de puntuación no solo sirve para medir el resultado final, sino que participa activamente en el proceso de razonamiento. Evalúa cada candidata, aporta feedback para las reparaciones, permite ordenar las trayectorias y guía la poda del \emph{Beam Search}.
Gracias a este componente, el sistema puede avanzar de manera iterativa hacia estrofas que cumplen cada vez mejor las restricciones formales definidas.


%────────────────────── Configuración del Entorno Experimental y Datasets ──────────────────────────

\section{Configuración del Entorno Experimental y Problema de Prueba}

Una vez definida la arquitectura del agente y la función de puntuación, es necesario describir el entorno en el que se ejecutan los experimentos y el tipo de problema utilizado para evaluar el sistema. En este trabajo se opta por una ejecución local del modelo de lenguaje,
utilizando \emph{Ollama} como herramienta para lanzar las inferencias desde el propio equipo de desarrollo.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Entorno de Ejecución}

La Tabla~\ref{tab:entornoExperimental} resume las principales características del entorno utilizado durante la ejecución de los experimentos.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Elemento} & \textbf{Configuración} \\
        \hline
        Sistema operativo & Windows 11 Home \\
        \hline
        Procesador & Intel Core Ultra 9 185H \\
        \hline
        Memoria RAM & 16 GB \\
        \hline
        GPU & Intel Arc Graphics \\
        \hline
        Entorno de desarrollo & Visual Studio Code \\
        \hline
        Lenguaje & Python \\
        \hline
        Modelo local & \texttt{mistral:7b-instruct} con Ollama \\
        \hline
    \end{tabular}
    \caption{Configuración del entorno experimental utilizado para ejecutar el agente. Fuente: elaboración propia}
    \label{tab:entornoExperimental}
\end{table}

El uso de un modelo local condiciona directamente el diseño experimental. A diferencia de una API externa, la ejecución en local permite un mayor control sobre el entorno y evita depender de servicios de terceros, pero también introduce limitaciones de tiempo y recursos.
Cada llamada al modelo tiene un coste computacional apreciable, especialmente cuando el sistema genera varias variantes por iteración o activa fases adicionales de reparación.

Por este motivo, los experimentos deben equilibrar la calidad de las soluciones generadas con el tiempo total de ejecución. Parámetros como el ancho del haz $k$, el número máximo de iteraciones o el número de variantes solicitadas en cada reparación influyen directamente en dicho coste.

% -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Configuración del Agente}

La configuración experimental del agente se basa en un conjunto de parámetros que controlan tanto la generación como la búsqueda. En las pruebas principales se utiliza un ancho de haz reducido, con el objetivo de mantener el coste computacional dentro de unos límites razonables.

La Tabla~\ref{tab:configuracionAgente} resume los principales parámetros utilizados durante la ejecución del sistema.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Parámetro} & \textbf{Valor utilizado} \\
        \hline
        Modelo generador & \texttt{mistral:7b-instruct} \\
        \hline
        Temperatura de generación & 0.3 \\
        \hline
        Número máximo de tokens & 600 \\
        \hline
        Ancho del haz & $k = 2$ \\
        \hline
        Número máximo de iteraciones & 3 \\
        \hline
        Reparación métrica inicial & Activada \\
        \hline
        Reparación de rima exterior A & Activada \\
        \hline
        Reparación métrica posterior a rima A & Activada \\
        \hline
        Reparación de rima interior B & Activada \\
        \hline
        Reparación métrica posterior a rima B & Activada \\
        \hline
    \end{tabular}
    \caption{Configuración principal del agente durante la fase experimental. Fuente: elaboración propia}
    \label{tab:configuracionAgente}
\end{table}

Además de estos parámetros generales, el sistema incorpora configuraciones específicas para las fases de reparación métrica y de rima. Estas fases pueden generar varias variantes de un verso concreto y aceptar únicamente aquellas que mejoran o mantienen el cumplimiento formal de la estrofa.
Esta configuración permite estudiar no solo la capacidad de generación inicial del modelo, sino también el efecto de los mecanismos de refinamiento introducidos en el agente.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Problema de Prueba: Generación de una Estrofa ABBA}

El problema seleccionado para evaluar el agente es la generación automática de una estrofa clásica en español formada por cuatro versos. La estrofa debe cumplir las siguientes restricciones:

\begin{itemize}
    \item contener exactamente cuatro versos;
    \item aproximarse al patrón endecasílabo;
    \item seguir un esquema de rima consonante ABBA.
\end{itemize}

Aunque en una fase inicial del proyecto se planteó la generación de un soneto completo, el desarrollo experimental se centra finalmente en la primera estrofa, con esquema de rima ABBA. Esta decisión no implica un cambio en la naturaleza del problema, sino una reducción controlada de su alcance.
En lugar de abordar directamente la generación completa de catorce versos, se estudia con mayor profundidad una unidad poética más pequeña, pero suficientemente representativa de las dificultades principales: control del número de versos, métrica endecasílaba y rima consonante.

Este enfoque permite analizar de forma más precisa el comportamiento del agente. Al trabajar con una única estrofa, resulta más sencillo identificar en qué punto aparecen los errores, qué efecto tienen las reparaciones locales y cómo influye el proceso de selección mediante \emph{Beam Search}.
De este modo, el sistema puede evaluarse sobre una tarea acotada, pero no trivial.

Además, el método propuesto es extensible a la generación de un soneto completo. Una vez resuelta la generación de una estrofa ABBA, el mismo esquema podría aplicarse a la segunda estrofa del soneto, también con rimas fijadas. Posteriormente, sería posible adaptar la lógica de evaluación y
reparación a los tercetos finales, cuyo esquema de rima puede adoptar patrones como CDC. Por tanto, la generación de la primera estrofa se plantea como una prueba representativa del método, no como una limitación conceptual del sistema.

La elección de este problema resulta adecuada para evaluar un sistema de razonamiento multietapa por varias razones. En primer lugar, no basta con generar texto gramaticalmente correcto: la salida debe satisfacer restricciones formales verificables. En segundo lugar, los errores
pueden aparecer en distintos niveles, como el número de versos, la métrica de un verso aislado o la rima entre dos versos concretos. Por último, una estrofa parcialmente incorrecta puede seguir siendo útil si contiene elementos aprovechables que puedan repararse posteriormente.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Justificación Experimental}

La generación de estrofas con restricciones constituye un caso de prueba interesante para el agente porque obliga a combinar creatividad lingüística y control formal. El modelo de lenguaje aporta capacidad generativa, mientras que el grafo de razonamiento introduce mecanismos de evaluación,
selección y reparación.

Este planteamiento permite observar si el proceso iterativo mejora realmente los resultados frente a una generación directa. En lugar de aceptar la primera salida del modelo, el sistema genera varias alternativas, las evalúa, conserva las más prometedoras y aplica reparaciones locales cuando
detecta errores. Por tanto, el problema permite estudiar el valor añadido de la arquitectura propuesta.

Además, el uso de trazas y métricas estructuradas permite comparar distintas configuraciones del sistema. Por ejemplo, se puede analizar el efecto de activar o desactivar la reparación métrica, añadir reparación de rima, modificar el ancho del haz o cambiar el número de iteraciones.
Estas comparaciones se desarrollan en los apartados posteriores, donde se estudia la evolución del agente a través de diferentes ciclos de experimentación.
























%────────────────────── Ciclo 1: Aproximación Inicial y Resultados Base ──────────────────────────
\section{Ciclo 1: Aproximación Inicial y Resultados Base}

El primer ciclo experimental tiene como objetivo evaluar una versión base del sistema, previa a la incorporación de los mecanismos avanzados de reparación. Esta versión mantiene la arquitectura general del agente, basada en generación de candidatas, evaluación formal y selección mediante \emph{Beam Search},
pero desactiva las fases específicas de reparación métrica, reparación de rima y protección de \emph{beams}.

De esta forma, el Ciclo 1 permite estudiar hasta qué punto la combinación de \emph{prompting}, evaluación objetiva y poda top-$k$ resulta suficiente para resolver el problema planteado. Esta configuración se utiliza como línea base para comparar posteriormente el impacto de las mejoras introducidas
en el sistema.

\subsection{Configuración de la Versión Base}

Para simular el comportamiento de la aproximación inicial, se desactivaron las principales fases de reparación incorporadas durante el desarrollo posterior. El Listado~\ref{lst:configCiclo1} muestra la configuración utilizada en este primer ciclo experimental.

\begin{lstlisting}[language=Python, caption={Configuración utilizada en el primer ciclo experimental}, label={lst:configCiclo1}]
ENABLE_LOCAL_METER_REPAIR = False
ENABLE_OUTER_RHYME_REPAIR = False
ENABLE_POST_A_RHYME_METER_REPAIR = False
ENABLE_INNER_RHYME_REPAIR = False
ENABLE_POST_B_RHYME_METER_REPAIR = False
ENABLE_BEAM_ELITISM = False
\end{lstlisting}

Con esta configuración, el sistema conserva el flujo general del grafo, pero no intenta corregir localmente los errores detectados. Las estrofas candidatas se generan, se evalúan mediante la función de puntuación formal y se seleccionan según su puntuación, pero no se aplican reparaciones
posteriores sobre versos concretos ni sobre parejas de rima.

Los parámetros generales utilizados en estas ejecuciones fueron los mismos que en la configuración experimental principal: modelo \texttt{mistral:7b-instruct}, temperatura 0.3, valor $k=2$ y un máximo de 3 iteraciones. Esto permite que la comparación posterior con la versión refinada se centre en el
efecto de las reparaciones, y no en cambios en los parámetros principales de ejecución.

\subsection{Resultados Cuantitativos}

La Tabla~\ref{tab:resultadosCiclo1} resume los resultados obtenidos en tres ejecuciones de la versión base. Aunque el número de ejecuciones es reducido, los resultados permiten identificar con claridad las limitaciones principales de esta primera aproximación.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Métrica} & \textbf{Resultado Ciclo 1} \\
        \hline
        Número de ejecuciones & 3 \\
        \hline
        Puntuación media final & 10.0/20 \\
        \hline
        Mejor puntuación obtenida & 12.0/20 \\
        \hline
        Peor puntuación obtenida & 8.0/20 \\
        \hline
        Estrofas con 4 versos & 100\% \\
        \hline
        Media de versos endecasílabos & 1/4 \\
        \hline
        Rima exterior A correcta & 0\% \\
        \hline
        Rima interior B correcta & 0\% \\
        \hline
        Media de cumplimiento de rima & 2/4 \\
        \hline
        Estrofas completamente válidas & 0\% \\
        \hline
        Tiempo medio de ejecución (segundos) & 475.83 s \\
        \hline
        Tiempo medio de ejecución (minutos) & 7.93 min \\
        \hline
    \end{tabular}
    \caption{Resultados generales obtenidos en el primer ciclo experimental con las reparaciones desactivadas. Fuente: elaboración propia}
    \label{tab:resultadosCiclo1}
\end{table}

Los resultados muestran que la versión base es capaz de respetar la estructura externa de la tarea, ya que en todas las ejecuciones se generan estrofas formadas por cuatro versos. Sin embargo, el cumplimiento de las restricciones formales más exigentes es muy limitado. La media de versos
endecasílabos es de solo 1 sobre 4, y ninguna ejecución consigue satisfacer correctamente la rima exterior A ni la rima interior B.

También resulta significativo que el cumplimiento de rima se mantenga en 2/4 en todas las ejecuciones. Este valor no implica que el esquema ABBA esté parcialmente resuelto, sino que los versos utilizados como referencia o ancla dentro del patrón se contabilizan como parte de la estructura de rima.
Las relaciones realmente relevantes, es decir, la correspondencia entre el verso 1 y el verso 4 para la rima A, y entre el verso 2 y el verso 3 para la rima B, fallan en todos los casos analizados.

\subsection{Ejemplo de Salida Obtenida}

El Listado~\ref{lst:ejemploCiclo1} muestra una de las salidas obtenidas durante este primer ciclo. Se ha seleccionado este ejemplo porque corresponde a la mejor puntuación observada en las ejecuciones realizadas, con un resultado de 12.0 sobre 20.

\begin{lstlisting}[caption={Ejemplo de salida obtenida en el primer ciclo experimental}, label={lst:ejemploCiclo1}]
1. El tiempo pasa, y yo me quedo atrás
2. En recuerdos, como un olvido mudo
3. Que en el corazón se alberga sueño
4. Y que no se puede deshacer la pena
\end{lstlisting}

Aunque la estrofa contiene exactamente cuatro versos y alcanza parcialmente la restricción métrica, no cumple el esquema de rima ABBA. La rima exterior falla porque el primer verso termina en ``atrás'', mientras que el cuarto termina en ``pena''. Del mismo modo, la rima interior tampoco se satisface,
ya que ``mudo'' y ``sueño'' no presentan rima consonante.

Este ejemplo refleja una característica habitual de la versión base: el modelo genera una salida lingüísticamente plausible, pero no controla de forma fiable las restricciones formales impuestas. La puntuación obtenida se debe principalmente al número correcto de versos y a algunos aciertos métricos,
no a un cumplimiento completo de la estructura poética objetivo.

\subsection{Observaciones del Primer Ciclo}

El primer ciclo experimental permite extraer una conclusión inicial clara: la arquitectura basada en \emph{Beam Search} organiza correctamente el proceso de búsqueda, pero no basta por sí sola para garantizar la generación de estrofas válidas. La evaluación formal permite identificar las mejores
candidatas dentro de las alternativas disponibles, pero si ninguna de ellas cumple adecuadamente las restricciones, la poda no puede corregir el problema por sí misma.

Por otro lado, el tiempo de ejecución de esta versión base es considerablemente menor que el de la versión completa con reparaciones activadas. El tiempo medio observado es de aproximadamente 7.93 minutos por ejecución. Esta diferencia se debe a que el sistema no realiza llamadas adicionales al modelo
para reparar versos, corregir rimas o ajustar la métrica tras una reparación.

Por tanto, este primer ciclo muestra un compromiso claro entre coste computacional y calidad formal. La versión base es más rápida, pero insuficiente para resolver el problema. Esta observación sirve como punto de partida para el análisis de errores del siguiente apartado y justifica la incorporación
posterior de mecanismos específicos de reparación.









%────────────────────── Discusión del Ciclo 1 y Análisis de Errores ──────────────────────────
\section{Discusión del Ciclo 1 y Análisis de Errores}

Los resultados del primer ciclo experimental muestran que la versión base del sistema no es suficiente para resolver de forma fiable la generación de estrofas con restricciones formales. Aunque el agente mantiene correctamente la estructura general de generación,
evaluación y poda, el modelo no cumple de manera consistente la métrica endecasílaba ni el esquema de rima ABBA.

El primer problema observado es la limitación del \emph{prompting} directo. Las instrucciones dadas al modelo son suficientes para conseguir una estructura externa de cuatro versos, pero no garantizan el cumplimiento de restricciones lingüísticas más precisas.
Esto se aprecia especialmente en la métrica, donde la mayoría de versos generados no alcanzan el patrón endecasílabo esperado.

El segundo problema aparece en la rima. Aunque el prompt indica explícitamente que la estrofa debe seguir un esquema ABBA, el modelo no planifica de forma fiable las palabras finales de los versos. En consecuencia, las relaciones importantes de rima,
es decir, la correspondencia entre el verso 1 y el verso 4, y entre el verso 2 y el verso 3, fallan en las ejecuciones analizadas.

También se observa que el \emph{Beam Search}, por sí solo, no corrige estos errores. El algoritmo puede seleccionar las mejores candidatas disponibles, pero si todas las alternativas generadas presentan fallos formales, la poda únicamente conserva la opción menos incorrecta.
Por tanto, el problema no está en la estructura de búsqueda, sino en la falta de mecanismos que permitan reparar localmente los errores detectados.

Otro aspecto relevante es la posible pérdida de avances parciales. Una estrofa puede mejorar en una restricción, por ejemplo en la métrica, pero empeorar en otra, como la rima. Sin una estrategia de protección de \emph{beams} prometedores, el sistema puede descartar candidatas útiles
o modificar partes que ya eran correctas.

Por último, el Ciclo 1 muestra un compromiso claro entre coste y calidad. La versión base es más rápida porque realiza menos llamadas al modelo, pero sus resultados son insuficientes. Esto justifica la incorporación de fases adicionales de reparación, aunque estas aumenten el tiempo
total de ejecución.

La Figura~\ref{fig:ErroresCiclo1} resume los principales errores detectados durante el primer ciclo experimental. Estos fallos muestran que la arquitectura base necesita mecanismos adicionales de reparación para poder satisfacer las restricciones formales de la estrofa.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.7\textwidth]{images/ErroresCiclo1.png}
   \caption{Principales errores observados durante el primer ciclo experimental. Fuente: elaboración propia}
    \label{fig:ErroresCiclo1}
\end{figure}

En conclusión, el análisis del primer ciclo permite identificar las principales necesidades de mejora del sistema: reparación métrica local, reparación específica de rima exterior e interior, reparación posterior conservando palabras finales válidas y preservación de \emph{beams} prometedores.
Estas mejoras constituyen la base del segundo ciclo experimental.






%────────────────────── Ciclo 2: Optimización y Refinamiento del Código ──────────────────────────

\section{Ciclo 2: Optimización y Refinamiento del Código}

Tras analizar los resultados del primer ciclo experimental, se comprobó que la arquitectura base del agente no era suficiente para garantizar el cumplimiento de las restricciones formales de la estrofa. Aunque el sistema era capaz de generar varias alternativas y seleccionar
las mejores mediante \emph{Beam Search}, los errores de métrica y rima seguían apareciendo de forma recurrente.

Por este motivo, el segundo ciclo de desarrollo se centró en introducir mecanismos de reparación específicos. El objetivo principal no fue sustituir la arquitectura inicial, sino complementarla con fases capaces de corregir errores locales sin regenerar toda la estrofa desde cero.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Activación de Mecanismos de Reparación}

La principal diferencia entre el Ciclo 1 y el Ciclo 2 se encuentra en la activación de las fases de reparación. En esta segunda versión, el agente no se limita a evaluar y podar candidatas, sino que intenta corregir determinados errores antes de calcular la puntuación final.

El Listado~\ref{lst:configCiclo2} muestra la configuración utilizada en esta versión refinada.

\begin{lstlisting}[language=Python, caption={Configuración utilizada en el segundo ciclo experimental}, label={lst:configCiclo2}]
ENABLE_LOCAL_METER_REPAIR = True
ENABLE_OUTER_RHYME_REPAIR = True
ENABLE_POST_A_RHYME_METER_REPAIR = True
ENABLE_INNER_RHYME_REPAIR = True
ENABLE_POST_B_RHYME_METER_REPAIR = True
ENABLE_BEAM_ELITISM = True
\end{lstlisting}

Con esta configuración, el nodo evaluador adquiere un papel más activo. Además de calcular la puntuación formal de cada candidata, puede aplicar reparaciones métricas, diagnosticar errores de rima y generar variantes locales para mejorar la estrofa.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Reparación Métrica Local}

La primera mejora incorporada fue la reparación métrica local. En lugar de pedir al modelo que reescriba toda la estrofa cuando uno o varios versos no son endecasílabos, el sistema identifica los versos problemáticos y solicita variantes únicamente para esos versos.

Esta estrategia reduce el riesgo de modificar partes de la estrofa que ya eran correctas. Si un verso cumple la métrica, se conserva; si falla, se intenta acercarlo al objetivo de once sílabas. De este modo, la reparación se aplica de forma más controlada y permite aprovechar
mejor las candidatas parcialmente válidas.

La reparación métrica no pretende resolver todos los problemas de la estrofa, sino actuar sobre una restricción concreta. Por tanto, no modifica deliberadamente la rima ni la estructura global, sino que se centra en mejorar el ajuste silábico de los versos detectados como incorrectos.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Reparación de la Rima Exterior e Interior}

El segundo bloque de mejoras se centró en la rima consonante ABBA. A partir de los errores observados en el Ciclo 1, se decidió tratar por separado las dos relaciones de rima de la estrofa:

\begin{itemize}
    \item \textbf{Rima exterior A}: relación entre el verso 1 y el verso 4.
    \item \textbf{Rima interior B}: relación entre el verso 2 y el verso 3.
\end{itemize}

En ambos casos se utiliza una estrategia de verso ancla. Para la rima exterior, el primer verso se conserva y el sistema intenta reescribir únicamente el cuarto. Para la rima interior, el segundo verso actúa como referencia y se modifica únicamente el tercero.

Esta decisión permite convertir un problema global en una reparación localizada. En lugar de regenerar la estrofa completa, el sistema conserva la parte que sirve como referencia y modifica solo el verso que debe adaptarse a ella. Con ello se reduce la probabilidad de perder
elementos que ya cumplían alguna restricción formal.

Además, el sistema utiliza palabras finales candidatas para orientar al modelo hacia terminaciones rimáticas más adecuadas. Esta ayuda resulta especialmente útil cuando el modelo falla al producir una rima consonante válida a partir de una instrucción abierta.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Reparación Métrica Posterior a la Rima}

Durante el desarrollo se observó un problema adicional: algunas variantes conseguían corregir la rima, pero introducían errores métricos. Es decir, el sistema podía obtener una palabra final adecuada para cumplir la rima, pero el verso resultante no alcanzaba las once sílabas métricas.

Para evitar perder esas mejoras parciales, se añadió una fase de reparación métrica posterior a la rima. La idea es conservar exactamente la palabra final que permitió corregir la rima y ajustar únicamente el resto del verso para mejorar su métrica.

Esta fase se aplica tanto a la rima exterior A como a la rima interior B. Su objetivo es combinar dos restricciones que inicialmente podían entrar en conflicto: mantener una terminación rimática válida y, al mismo tiempo, acercar el verso al patrón endecasílabo.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Protección de \emph{Beams} Prometedores}

Otra mejora introducida en el segundo ciclo fue la protección de \emph{beams} prometedores. En la versión base, una estrofa con buena puntuación podía desaparecer si las variantes generadas a partir de ella eran peores. Esto provocaba la pérdida de soluciones parciales útiles.

Para reducir este problema, se añadió una estrategia de elitismo. Esta técnica permite conservar determinados \emph{beams} de alta calidad aunque sus descendientes inmediatos no mejoren la puntuación. De esta forma, el sistema evita perder una candidata valiosa por una mala generación
puntual del modelo.

La protección de \emph{beams} resulta especialmente importante en este tipo de tareas, donde las mejoras no siempre son progresivas. Una reparación puede corregir una rima pero empeorar la métrica, o mantener la estructura pero alterar una palabra final útil.
El elitismo aporta mayor estabilidad al proceso de búsqueda.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Resumen de las Mejoras Introducidas}

La Tabla~\ref{tab:mejorasCiclo2} resume las principales mejoras incorporadas en el segundo ciclo y el problema que intenta resolver cada una de ellas.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|l|}
        \hline
        \textbf{Mejora introducida} & \textbf{Problema que aborda} \\
        \hline
        Reparación métrica local & Versos que no alcanzan 11 sílabas \\
        \hline
        Reparación de rima exterior A & Fallo de rima entre versos 1 y 4 \\
        \hline
        Reparación de rima interior B & Fallo de rima entre versos 2 y 3 \\
        \hline
        Reparación métrica post-rima & Rimas corregidas con versos métricamente incorrectos \\
        \hline
        Protección de \emph{beams} & Pérdida de candidatas prometedoras \\
        \hline
    \end{tabular}
    \caption{Principales mejoras incorporadas durante el segundo ciclo de desarrollo. Fuente: elaboración propia}
    \label{tab:mejorasCiclo2}
\end{table}

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Resultados del Segundo Ciclo}

Una vez activadas las fases de reparación y la protección de beams, se realizaron nuevas ejecuciones del sistema con la misma configuración general utilizada en el primer ciclo: modelo \texttt{mistral:7b-instruct}, temperatura 0.3,
valor $k=2$ y un máximo de 3 iteraciones. La diferencia principal se encuentra en que, en este segundo ciclo, el agente sí puede aplicar reparaciones métricas, reparaciones de rima y estrategias de preservación de beams prometedores.

La Tabla~\ref{tab:resultadosCiclo2} resume los resultados obtenidos en tres ejecuciones representativas del segundo ciclo experimental.

\begin{table}[h]
    \centering
    \begin{tabular}{|l|c|}
        \hline
        \textbf{Métrica} & \textbf{Resultado Ciclo 2} \\
        \hline
        Número de ejecuciones & 3 \\
        \hline
        Puntuación media final & 18.67/20 \\
        \hline
        Mejor puntuación obtenida & 20.0/20 \\
        \hline
        Peor puntuación obtenida & 18.0/20 \\
        \hline
        Estrofas con 4 versos & 100\% \\
        \hline
        Media de versos endecasílabos & 4/4 \\
        \hline
        Rima exterior A correcta & 100\% \\
        \hline
        Rima interior B correcta & 33.33\% \\
        \hline
        Media de cumplimiento de rima & 3.33/4 \\
        \hline
        Estrofas completamente válidas & 33.33\% \\
        \hline
        Tiempo medio de ejecución & 2841.13 s \\
        \hline
        Tiempo medio de ejecución & 47.35 min \\
        \hline
    \end{tabular}
    \caption{Resultados generales obtenidos en el segundo ciclo experimental con las reparaciones activadas. Fuente: elaboración propia}
    \label{tab:resultadosCiclo2}
\end{table}

Los resultados muestran una mejora clara respecto al primer ciclo experimental. La puntuación media aumenta de forma significativa y, en una de las ejecuciones, el sistema alcanza una puntuación formal perfecta de 20.0 sobre 20.
Además, todas las estrofas generadas mantienen la estructura de cuatro versos y alcanzan el objetivo métrico de cuatro versos endecasílabos.

La mejora también se aprecia en la rima. La rima exterior A se satisface en todas las ejecuciones analizadas, mientras que la rima interior B continúa siendo la restricción más difícil de resolver.
Este comportamiento confirma que la separación entre rima exterior e interior resulta útil, aunque la rima B sigue presentando mayor dificultad y requiere un tratamiento especialmente cuidadoso.

No obstante, esta mejora formal tiene un coste computacional elevado. El tiempo medio de ejecución aumenta hasta aproximadamente 47.35 minutos. Este incremento se debe a las llamadas adicionales al modelo durante las fases de
reparación métrica, reparación de rima y reparación métrica posterior a la rima. Por tanto, el segundo ciclo pone de manifiesto un compromiso central del sistema: la calidad formal mejora considerablemente, pero a cambio de un mayor
tiempo de ejecución.

% --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\subsection{Ejemplo de Salida Obtenida}

El Listado~\ref{lst:ejemploCiclo2} muestra una de las salidas obtenidas durante el segundo ciclo experimental. En este caso, la estrofa alcanza una puntuación formal perfecta de 20.0 sobre 20, cumpliendo las restricciones de número de versos, métrica endecasílaba y rima consonante ABBA.

\begin{lstlisting}[caption={Ejemplo de salida obtenida en el segundo ciclo experimental}, label={lst:ejemploCiclo2}]
1. El tiempo, un río de continua corriente
2. Deja en mi corazón penas amargas.
3. Largo y triste, el viento abre sus llagas
4. Narrando la historia, llega al presente.
\end{lstlisting}

En este ejemplo, la estrofa contiene exactamente cuatro versos y todos ellos son evaluados como endecasílabos por la función de puntuación. Además, se cumple el esquema de rima ABBA: el primer verso rima con el cuarto mediante las terminaciones ``corriente'' y ``presente'',
mientras que el segundo verso rima con el tercero mediante ``amargas'' y ``llagas''.

Este caso ilustra el efecto de las mejoras introducidas en el segundo ciclo. A diferencia de las salidas del Ciclo 1, el sistema no solo genera una estrofa con apariencia poética, sino que consigue satisfacer completamente las restricciones formales definidas para el problema.


En conjunto, el Ciclo 2 transforma el sistema inicial en un agente más robusto. La arquitectura sigue basándose en generación, evaluación y poda, pero ahora incorpora mecanismos de diagnóstico y reparación que permiten aprovechar mejor las estrofas parcialmente correctas.
Los resultados obtenidos muestran que estas mejoras incrementan de forma clara la puntuación formal, aunque también aumentan considerablemente el tiempo de ejecución. Esta relación entre calidad y coste será analizada de forma comparativa en el apartado final del capítulo.














%────────────────────── Evaluación Comparativa Final y Discusión Global ──────────────────────────
\section{Evaluación Comparativa Final y Discusión Global}



%\section{Quantitative metrics}
%\begin{table}[ht]
%  \centering
%  \caption{SR and segmentation performance (mean \(\pm\) std).}
%  \label{tab:metrics}
%  \begin{tabular}{l S[table-format=2.2] S[table-format=2.2] S[table-format=1.3]}
%    \toprule
%    Method & {PSNR [dB]} & {SSIM} & {Dice} \\
%    \midrule
%    Bicubic + RF     & 28.31 & 0.842 & 0.786 \\
%    EDSR + U\textsuperscript{3}Net & 31.75 & 0.904 & 0.865 \\
%    Proposed (joint) & 32.18 & 0.918 & 0.882 \\
%    \bottomrule
%  \end{tabular}
%\end{table}

%
%\section{Qualitative examples}
%\begin{figure}[ht]
%  \centering
%  \includegraphics[width=.9\linewidth]{example-image-b}
%  \caption{Axial slices: LR input, SR output, and segmentation overlay.}
%  \label{fig:qualitative}
%\end{figure}

%────────────────────── Chapter 5 — Discussion ─────────────────────────
\chapter{Discussion}
Joint optimization improves boundary fidelity and suppresses ringing.
Failure cases include motion artifacts and unseen acquisition protocols.

%────────────────────── Appendices ─────────────────────────────────────
\begin{umaappendices}

\umaappendix{Data splits and preprocessing}
\section{Preprocessing}
Bias-field correction, z-score normalization per volume, random flips and
intensity jitter for augmentation.

\umaappendix{Additional code snippets}

\section{Evaluation script (Python)}
\begin{lstlisting}[style=python,caption={Metric computation script},label={lst:metrics}]
# eval_metrics.py
# Computes PSNR, SSIM (skimage), and Dice given predictions and references.

from skimage.metrics import structural_similarity as ssim
import numpy as np

def psnr(x, y, peak=1.0):
    mse = np.mean((x - y) ** 2)
    return 10 * np.log10(peak**2 / (mse + 1e-12))

def dice(p, m, thr=0.5):
    pbin = (p >= thr).astype(np.float32)
    m = m.astype(np.float32)
    return 2*(pbin*m).sum() / (pbin.sum() + m.sum() + 1e-12)
\end{lstlisting}

\section{R quick-look plot}
\begin{lstlisting}[style=r,caption={Boxplots of PSNR and Dice},label={lst:rplots}]
# rplots.R - Statistical visualization of model performance
library(ggplot2)
library(dplyr)

# Generate synthetic results for demonstration
df <- data.frame(
  method = rep(c("Bicubic+RF","EDSR+U3Net","Joint"), each=10),
  psnr = c(rnorm(10,28.3,0.6), rnorm(10,31.8,0.5), rnorm(10,32.2,0.4)),
  dice = c(rnorm(10,0.786,0.02), rnorm(10,0.865,0.02), rnorm(10,0.882,0.02))
)

# Create visualization pipeline
df %>%
  ggplot(aes(method, psnr, fill=method)) +
  geom_boxplot() +
  theme_minimal() +
  labs(title="PSNR by Method", y="PSNR (dB)")
\end{lstlisting}

\section{C++ performance-critical kernels}
\begin{lstlisting}[style=cpp,caption={CUDA-style convolution kernel},label={lst:cpp}]
// conv3d_kernel.cpp - High-performance 3D convolution
#include <vector>
#include <cmath>
#include <algorithm>

template<typename T>
class Conv3DKernel {
private:
    std::vector<T> weights_;
    int kernel_size_;

public:
    Conv3DKernel(int size) : kernel_size_(size) {
        weights_.resize(size * size * size);
    }

    // Apply convolution at a single point
    T apply(const T* input, int x, int y, int z, int stride) const {
        T sum = static_cast<T>(0);
        for (int dz = 0; dz < kernel_size_; ++dz) {
            for (int dy = 0; dy < kernel_size_; ++dy) {
                for (int dx = 0; dx < kernel_size_; ++dx) {
                    int idx = (z + dz) * stride * stride
                            + (y + dy) * stride + (x + dx);
                    sum += input[idx] * weights_[dz * kernel_size_ * kernel_size_
                                                + dy * kernel_size_ + dx];
                }
            }
        }
        return sum;
    }
};
\end{lstlisting}

\section{SQL data pipeline}
\begin{lstlisting}[style=sql,caption={Patient cohort selection query},label={lst:sql}]
-- cohort_selection.sql
-- Select patients with complete MRI sequences for training
SELECT DISTINCT
    p.patient_id,
    p.age,
    p.diagnosis_date,
    COUNT(s.scan_id) AS num_scans
FROM patients p
INNER JOIN mri_scans s ON p.patient_id = s.patient_id
WHERE s.modality = 'T1w'
  AND s.quality_score >= 0.8
  AND p.diagnosis_date BETWEEN '2020-01-01' AND '2024-12-31'
GROUP BY p.patient_id, p.age, p.diagnosis_date
HAVING COUNT(s.scan_id) >= 3
ORDER BY p.diagnosis_date DESC
LIMIT 1000;
\end{lstlisting}

\section{Bash automation}
\begin{lstlisting}[style=bash,caption={Training pipeline automation},label={lst:bash}]
#!/bin/bash
# train_pipeline.sh - Automated model training and evaluation

# Configuration
MODEL_DIR="./checkpoints"
DATA_DIR="/data/mri_volumes"
EPOCHS=100

# Create output directories
mkdir -p "$MODEL_DIR" logs

# Train with different learning rates
for lr in 1e-3 1e-4 1e-5; do
    echo "Training with lr=$lr"
    python train.py \
        --data "$DATA_DIR" \
        --lr "$lr" \
        --epochs "$EPOCHS" \
        --output "$MODEL_DIR/model_lr${lr}.pt" \
        2>&1 | tee "logs/train_lr${lr}.log"
done

# Evaluate best model
echo "Evaluating models..."
python evaluate.py --checkpoint "$MODEL_DIR" --metrics psnr ssim dice
\end{lstlisting}

\end{umaappendices}

%────────────────────── Back cover and bibliography ────────────────────
\backmatter
\printbibliography[title={Bibliografía}]
\cleardoublepage
\MakeUMABackCover

\end{document}
% ======================================================================
